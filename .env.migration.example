# 仅供部署人员执行 PostgreSQL 迁移；不要加入网站 systemd EnvironmentFile。
# 真实文件必须限制为部署账号可读（chmod 600），且不得提交 Git。
MIGRATION_DATABASE_URL=postgresql://data_statistics_migrator:MIGRATION_PASSWORD@HOST:5432/data_statistics?schema=public
