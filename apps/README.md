# 前端应用目录

这个仓库保存整套线上系统，不再只保存后端。

- 仓库根目录：后端 API、数据库迁移和旧版兼容页面。
- `apps/frontline`：新版员工端，线上监听 `3000` 端口。
- `apps/admin`：新版管理端，线上监听 `3002` 端口。
- 根目录后端：线上监听 `3003` 端口。

生产发布时，三套源码对应到同一个发布目录的 `frontline`、`admin`和 `backend`。生产环境文件、数据库和构建产物不提交到 Git。
