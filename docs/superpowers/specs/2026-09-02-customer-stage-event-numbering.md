# 客户阶段事件编号与号码统计口径

> 本文是 2026-09-02 阶段编号功能的实施记录。持续变化的权限、部署状态和完整业务口径以 [`docs/business/current-business-rules.md`](../../business/current-business-rules.md) 为准。

## 已确认的业务决定

1. 所有日期的“进群数量”输入框锁定，组员、组长和 AI 都不能直接修改进群数字。
2. 清空 2026-09-01 和 2026-09-02 原手工填写的进群数字，但不删除已经存在的客户档案、号码和进度。
3. 从 2026-09-01 起，进群、推专家、注册、开单和退群均以客户号码对应的有效阶段事件统计。
4. 2026-09-01 以前的旧进群数字保留为只读历史，不因新口径被清空。
5. 组员用本人账号补录号码，业务永久归原接粉人；炒群负责人和专家只记录执行进度。
6. 部门、公司和总公司管理员只读，不能代录客户；实际一线负责人或本组组长新增时必须明确选择真实接粉归属。

## 阶段编号

展示格式为：`阶段字母-月-日-当日三位流水号`。

- 进群：`G-9-1-001`
- 推专家：`E-9-1-001`
- 注册：`R-9-1-001`
- 开单：`O-9-1-001`
- 退群：`L-9-1-001`

每个小组、每个阶段、每个自然日独立从 `001` 开始。页面不显示年份，但数据库必须保存完整的 `YYYY-MM-DD` 和小组 ID，保证跨年、跨小组不重复。流水号由原子计数器分配，不得用“当前行数 + 1”。

## 在群与退群

- “在群”是客户当前状态，不生成新编号，继续使用原 `G` 编号。
- 正常退群和异常退群共用 `L` 编号，退群类型作为独立字段。
- 纠正退群类型时不更换 `L` 编号。

## 日报和月报统计

统计依据是“有效阶段事件的条数”，不是最大流水号。

- 当日进群：事件日期为当日的有效 `G` 记录数。
- 当日推专家、注册、开单和退群：分别统计当日有效 `E/R/O/L` 记录数。
- 当前在群：已有有效 `G` 且没有有效 `L` 的客户数，不把每天快照相加。
- 当月汇总：统计事件日期落在当月的有效记录。
- 事件日期跨月纠正时，从原月份减 `1`，向新月份加 `1`。
- 首充、续充、出金和净业绩继续以组员填写的公司最终认账数字为准，客户阶段记录不重复入账。

## 客户进度日期筛选

客户进度页顶部增加“月份”和“日”两个下拉选择器，与小组、渠道和进度筛选同时生效。

- 只选月份、“日”选“整月”：查看该月全部客户。
- 选择月份和具体日：只查看该天客户。
- 在群待推专家：按 `G` 进群事件日期筛选。
- 已推专家／专家进度：默认按 `E` 推专家事件日期筛选。
- 选中“已注册”状态：按 `R` 注册事件日期筛选。
- 选中“已开单”状态：按 `O` 开单事件日期筛选。
- 选中退群状态：按 `L` 退群事件日期筛选。
- 更换月份后，“日”下拉只显示该月有效的日期，默认回到“整月”。

## 纠错规则

- 号码可以纠错，但必须检查全局重复，并记录修改前号码、修改后号码、操作人和时间。
- 号码纠错不得丢失客户 ID、归属人、阶段编号、推专家、注册、开单和资金跟踪。
- 日期纠错时，统计必须从原日期撤销，再计入新日期；旧编号保留在操作日志中。
- 作废记录不参与日报、月报和当前在群统计。
- 选择专家负责人时必须同时提交实际推专家日期；默认当前统计日。
- 推专家日期必须不早于进群日期，也不能晚于已经存在的退群、注册或开单日期。
- 推专家日期纠错时，炒群端“推专家”和专家端“接收客户”必须同时从旧日期减 1、在新日期加 1，并按新日期取得 E 编号。

## 权限边界

- 组员和组长可以在本组职责范围内推进客户；普通组员只有开通专家权限后才能编辑专家阶段。
- 部门、公司和总公司管理员可以按范围只读查看客户，但不能代录、新增或修改客户数据。
- 资源部不能查看或修改客户进度，只能读取明确授权的渠道 ID 对应的数据；相同渠道类型不代表自动获得权限。
- 财务和人事不能修改客户进度；AI 也必须沿用当前账号的同一套权限。

## 2026-09-01 与 2026-09-02 补录目标

- 报表目标：9 月 1 日进群 6 人，9 月 2 日进群 7 人，合计 13 人。
- 清空这两天的手工进群数字后，已有号码记录共 7 人，组员需再补齐 6 人。
- 补录时必须自动识别已存在号码，已存在记录进入纠错流程，不得创建重复客户。

## 完成时状态快照

- 已在 `codex/ai-chat-data-entry` 分支完成所有日期的手填进群锁定，以及 G/E/R/O/L 按小组、阶段、业务日期原子编号。
- 已完成号码导入、归属接粉人、炒群/专家只记录执行进度、注册/开单/退群事件统计和号码重复纠错。
- 已完成客户进度月份、日期、状态筛选，以及页面 15 秒自动刷新和窗口重新聚焦刷新。
- 已提供只清理 2026-09-01、2026-09-02 手填进群值的安全脚本；默认仅预演，必须显式 `--apply` 才写入。
- 本地已走通进群、推专家、注册、开单、退群完整链路，并验证报表归属接粉人、专家金额不进入公司认账业绩。
- 本节只记录功能完成时的状态，不能用于判断当前是否已经部署；生产状态必须以实际版本和运维记录核对。
- 当前业务总口径统一记录在 `docs/business/current-business-rules.md`；旧计划与旧页面说明如有冲突，以该文件为准。
