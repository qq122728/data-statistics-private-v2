# 最终录入流程修复报告

## 结果

- “拉群与退群”现在一行只选择一次来源批次和发生日期，并同时显示“拉群数量”“退群数量”。
- “转化与充值”现在一行只选择一次来源批次和发生日期，并同时显示“专家介绍数量”“注册数量”“开单人数”“充值金额（元）”。
- 每个数字框都允许留空；留空的指标不会生成事件。`0` 是有效数据，会正常生成值为零的事件。
- 一行填写的多个指标会在一次 `POST /api/events` 中提交。现有 API 已经先校验整批事件，再使用数据库事务统一写入，因此没有修改 API：任一事件无效时，同一行的其他事件也不会偷偷落库。
- API 返回的事件序号错误会重新映射到原表单行和具体数字框；错误框保留输入值，并继续使用 `aria-invalid`、`aria-describedby` 和可见错误消息。

## 根因

旧版两个表单把每行建模为“类型 + 一个值”，所以用户必须为同一来源批次和同一发生日期重复添加多行。API 本身已经支持事件数组和事务回滚，问题只在表单没有把一个业务行展开成多个事件，也没有保存“展开后的事件序号对应哪个输入框”的信息。

## TDD 证据

先改浏览器测试，再改实现。首次运行新场景时，测试在等待“拉群数量”输入框时超时；完成拉群表单后，测试继续在等待“专家介绍数量”输入框时超时。这两个红灯分别证明旧版两个表单不满足单行多指标要求。

绿灯场景覆盖：

- 单个拉群/退群行中一个值无效时，数据库事件数保持为 `0`，证明没有部分写入。
- 修正同一行后，一次响应包含 `GROUP_JOIN` 和 `GROUP_LEAVE` 两个事件，包括合法的零值。
- 单个转化行一次响应包含 `EXPERT_INTRO`、`REGISTRATION`、`ORDER`、`RECHARGE` 四个事件，`12.34` 元保存为 `1234` 分。
- 稀疏转化行只填写注册 `0`、其他字段留空时，只生成一个注册事件。
- 负数和三位小数充值错误都关联到各自输入框并保留原值。

## 最终验证

- `npm run test:e2e -- tests/e2e/data-entry.spec.ts`：8 个测试全部通过。
- `npm test -- --run`：10 个测试文件、38 个单元测试全部通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：Next.js 生产构建通过。
- `git diff --check`：通过。

## 本次限定文件

- `src/components/entry/GroupChangesForm.tsx`
- `src/components/entry/ConversionForm.tsx`
- `src/components/entry/errors.ts`
- `tests/e2e/data-entry.spec.ts`
- `final-entry-report.md`

未修改或提交 history、admin、reports 范围文件；工作区中这些目录的并行改动属于其他任务。
