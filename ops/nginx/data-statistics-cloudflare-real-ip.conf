# NET-02 managed Cloudflare real IPs; the synchronization job replaces this file.
# With no set_real_ip_from entries, untrusted direct clients cannot supply the
# visitor address. Install and include this bootstrap before the first sync.
real_ip_header CF-Connecting-IP;
real_ip_recursive on;
