[Service]
# Prevent a request flood from filling journald through application output.
# systemd emits its own suppressed-message count when this limit is exceeded.
LogRateLimitIntervalSec=30s
LogRateLimitBurst=1000
