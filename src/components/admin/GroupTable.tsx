import { CheckCircle, Circle } from "@phosphor-icons/react";

export type ManagedGroup = { id: string; name: string; active: boolean; departmentId: string; departmentName: string; timezone: string | null; inheritedTimezone: boolean; effectiveTimezone: string; effectiveWorkStartMinutes: number; effectiveWorkEndMinutes: number; localTime: string; label: string; memberCount: number; channelCount: number };

export function GroupTable({ groups, onEdit }: { groups: ManagedGroup[]; onEdit: (group: ManagedGroup) => void }) {
  return <div className="overflow-x-auto border border-slate-200 bg-white"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-50 text-sm uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">下属公司</th><th className="px-4 py-3">小组名称</th><th className="px-4 py-3">当地时间</th><th className="px-4 py-3">工作状态</th><th className="px-4 py-3">成员数</th><th className="px-4 py-3">渠道数</th><th className="px-4 py-3">状态</th><th className="px-4 py-3 text-right">操作</th></tr></thead><tbody className="divide-y divide-slate-200">{groups.map((group) => <tr key={group.id} className="hover:bg-slate-50"><td className="px-4 py-4 text-slate-600">{group.departmentName}</td><td className="px-4 py-4 font-medium text-slate-950">{group.name}<small className="block font-normal text-slate-500">{group.inheritedTimezone ? "继承公司时区" : "小组独立时区"}</small></td><td className="px-4 py-4 text-slate-600">{group.localTime}</td><td className="px-4 py-4"><span className="font-medium">{group.label}</span><small className="block text-slate-500">10:00–22:00</small></td><td className="px-4 py-4 text-slate-600">{group.memberCount}</td><td className="px-4 py-4 text-slate-600">{group.channelCount}</td><td className="px-4 py-4"><span className={`inline-flex items-center gap-2 ${group.active ? "text-blue-700" : "text-slate-500"}`}>{group.active ? <CheckCircle size={17} weight="fill" aria-hidden="true" /> : <Circle size={17} aria-hidden="true" />}{group.active ? "启用" : "停用"}</span></td><td className="px-4 py-4 text-right"><button type="button" onClick={() => onEdit(group)} className="rounded px-3 py-1.5 font-medium text-blue-700 hover:bg-blue-50">编辑</button></td></tr>)}</tbody></table>{!groups.length && <div className="p-12 text-center text-sm text-slate-500">没有符合条件的小组</div>}</div>;
}
