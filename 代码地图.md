# 代码地图

> 当前业务口径以 [`docs/business/current-business-rules.md`](docs/business/current-business-rules.md) 为准。本文 2026-09-02 以前的“阶段”段落是重写过程记录；如果历史描述与当前业务文档冲突，以当前业务文档和现用 3000/3002 前端为准。

---

## 项目结构

```
apps/frontline/          ← 当前公网主前端（3000）：组员、资源部、部门/公司/总公司工作台
apps/admin/              ← 当前管理前端（3002）：组长及组织管理工作台
src/app/api/             ← 权威后端接口（3003）；根项目不再承载业务页面
src/lib/                 ← 认证、权限、统计、客户阶段、导出和业务时间
prisma/schema.prisma     ← SQLite 本地开发 schema
prisma/postgres/         ← PostgreSQL 生产 schema 与迁移
tests/unit/              ← 后端、统计与权限测试
apps/frontline/test/     ← 当前前端结构、交互与实时刷新测试
docs/business/           ← 当前有效业务规则
docs/superpowers/        ← 历史设计、计划和实施记录
```

旧单体前端 `src/app/(app)`、`src/components`、根登录页及其旧 E2E 测试已于 2026-09-02 删除。根项目构建结果现在只有 `/api/*` 和系统 `_not-found`，不会再出现第二套同名业务页面。

### 当前客户号码统计主链路

- 页面：`apps/frontline/components/DepartmentCustomerProgress.tsx`
- 读写接口：`src/app/api/lead/customer-reporting/route.ts`、`src/app/api/lead/customer-reporting/[leadId]/route.ts`
- G/E/R/O/L 编号：`src/lib/customer-stage-number.ts`
- 阶段数字同步和日期纠错：`src/lib/customer-number-event-sync.ts`
- 号码统计后禁用手填：`src/lib/customer-number-tracking.ts`
- 当前业务规则：`docs/business/current-business-rules.md`

### 当前权限主链路

- 多岗位识别：`src/lib/role-access.ts`
- 客户和通用报表边界：`src/lib/permissions.ts`
- 组织层级范围：`src/lib/org-permissions.ts`、`src/lib/report-scope.ts`
- 资源部专用数据入口：`src/app/api/resource/*`
- 资源部严格按 `ResourceChannelAccess.channelId` 授权，不能按 ADS/SMS 类型扩展到其他渠道，也不能进入客户进度接口。

---

## 数据模型地图（当前状态，重写前）

**组织架构**：`Company`(阶段1新增) → `Department`(含 countryCode/timezone，=国家，新增可空 `companyId`) → `TeamGroup`(含转化率阈值配置)。旧 Department 行 `companyId` 为 null，UI 尚未接入；阶段5a新增 `User.companyId`(可空，FK→Company，只给 `Duty.COMPANY_MANAGER` 账号用，null=未设置=无权限而非"全部")，`org-permissions.ts`/`org/`路由已经能建公司、给公司挂部门，但阶段5b前端接入之前，实际生产数据里公司层级还是空的——"真正挂上"仍要等5b的组织管理UI上线后管理员们实际操作。

**人员与角色**：`User.role`(单一 enum，含 ADMIN/RESOURCE_MANAGER/COMPANY_MANAGER/FINANCE/HR/LEAD/RECEPTION/GROUP_OPERATOR/EXPERT，岗位职务混在一起，**暂未废弃**) + `User.duty`(阶段1新增可空 `Duty` enum，管人的"帽子"，**阶段2起由 `transferUserPosition` 维护**：转成 LEAD 时置为 `"LEAD"`，转成其他工作岗位时清空) + `UserRoleAssignment`(额外岗位) + `UserGroupMembership`(调组历史表，**暂未废弃，仍由 transferUserPosition 同步写入**，供 `role-rankings.ts`/管理后台"调动履历"展示等现有消费方使用) + `UserPosition`(阶段1新增，**强制的岗位历史表**，唯一约束 `[userId, effectiveFrom]`，新增 `secondaryPositions` 字段镜像 `UserGroupMembership.secondaryRoles` 的逗号分隔写法——**阶段2起由 `transferUserPosition` 唯一写入**：LEAD 是职务不写 Position 行，只有 RECEPTION/GROUP_OPERATOR/EXPERT 才开新行)。`Position` enum(RECEPTION/GROUP_OPERATOR/EXPERT) 与 `Duty` enum(LEAD/DEPARTMENT_MANAGER/COMPANY_MANAGER/HQ_MANAGER/RESOURCE_MANAGER/FINANCE) 阶段1新增。

**转组转岗唯一入口**：[user-position/transfer.ts](src/lib/user-position/transfer.ts) 的 `transferUserPosition`——阶段2从 `admin/users/transfer/route.ts` 里整体抽出（原逻辑逐行保留，行为不变，仅新增 UserPosition 写入 + duty 维护 + 下面这条专家放弃状态处理），是 ADMIN/COMPANY_MANAGER 在用的真实调岗API的底层实现，不是新增的空跑函数。转岗前的交接校验（未交接在办客户则拒绝）逻辑原样保留；新增：专家名下"未成交/停止维护"(`STALLED`/`DECLINED_DEPOSIT`)客户视为已放弃跟进的终态，转岗时**自动划给原组在职组长**（不询问理由，找不到组长则原样保留原专家名下），不再占用"在办"计数、不阻挡转岗——这是阶段1审查发现的既存缺口（旧逻辑把这类客户误判为待开单在办客户）之外的另一处独立修正，此处是新设计不是bug修复。

**客户主记录**：`LeadCustomer`——接粉/炒群/专家三段状态都在同一张表上（`groupStatus`、`expertWorkflowStage` 等），`ownerId/expertOwnerId/groupOperatorOwnerId/attributionOwnerId` 都是**活外键**（指向 User 当前状态，不冻结——这是客户记录本身的所有权，不受阶段2冻结机制影响，`transferUserPosition` 的交接逻辑负责在转岗时把这些活外键批量转移给接手人）。已有 `isHistoricalRecord` 系列字段支持历史补录分段，阶段1新增 `historicalReviewStatus/historicalReviewedById/historicalReviewedAt`(审核字段，模式抄自 `InvalidFanReport`)，目前也还没有写入方，阶段6要落地“各岗位按阶段自助认领、组长审核后生效”的 API 才会用上。`receptionCategory`(PENDING/VALID/INVALID/LOW_AMOUNT/NO_WS) 支持无效库——**阶段1已验证并修复**：[mutations.ts](src/lib/customer-workflow/mutations.ts) 原来只要客户有回复/入群等下游进度就拒绝改判为 LOW_AMOUNT/NO_WS，与需求文档3.1.1的典型场景（跟进过程中才发现问题）冲突，已放开到"只在客户已进入专家环节或已开单时才拒绝"（因为那时候改判无效会连带锁死后续专家操作，是真实的功能冲突）；回复/入群阶段的客户改判无效不受影响，符合"仍可继续跟进"。`canonical-events.ts` 已正确把非VALID客户排除出统计口径，恢复路径(`restoreValid`/`classifyReception`→VALID)也已存在，均已验证。

✅ **误录作废护栏已修复（2026-08-29）**：[mutations.ts](src/lib/customer-workflow/mutations.ts) 的 `voidErroneousEntry` 已复用 `hasExpertOrOrderProgress`：客户一旦推专家、被专家联系、注册或存在有效开单，就拒绝按“误录作废”；仍处于回复/入群阶段的真实误录可以正常作废。回归测试覆盖四种下游进度，避免作废后把专家和订单流程锁死。

**订单与资金**：`CustomerOrder`(开单记录，1对1 LeadCustomer) + `MetricEvent`(多态事件表，`kind` 区分漏斗计数/入金/出金/续充，`enteredById` 固定不变、`voidedAt` 软删模式，阶段2新增 `actingPosition` 冻结字段)。**唯一写入口**：[metric-events.ts](src/lib/metric-events.ts) 的 `recordMetricEvent`/`recordMetricEvents`——全仓库9个 `metricEvent.create`/`createMany` 调用点已阶段2全部改走这两个函数，禁止再直接 `db.metricEvent.create`。`actingPosition` 冻结的是 `enteredById` 这个人**写入当下**持有的 `UserPosition`（查 `effectiveTo: null` 的那一行），不是"事件发生时"（`occurredOn` 可能是补录的历史日期）。⚠️ `enteredById` 语义本身在9个调用点并不统一（多数是当前操作人，但 `leads/route.ts` 导入用的是"粉的归属人"、`customer-workflow/service.ts` 的删除冲正用的是"客户原owner"）——这次没有改动这些既有语义，`actingPosition` 只是照抄各调用点已经在用的 `enteredById` 去查岗位，没有引入新的口径判断。目前多数账号还没有 `UserPosition` 记录（阶段2刚起步，只有走过 `transferUserPosition` 的账号才有），`actingPosition` 大量为 `null` 属正常。

**批次与渠道**：`SourceBatch`(冻结快照的范例——建批次时复制 `Channel` 当前的 `channelType`) + `Channel`(活的渠道配置)。阶段3 Stage B 已删除两者的成本/定价字段（`fanCostMode`/`effectiveFanPriceCents`/`rebateRateBps` 及 `SourceBatch` 对应的三个快照字段、三个投流广告字段、`FanCostMode` enum）；`channelType`/`channelTypeSnapshot` 按需求文档4.2保留，继续用于来源分类、客户编号前缀、资源部渠道权限等跟钱无关的用途。

**审核类**：`InvalidFanReport`(批次级废号审核，`PENDING/APPROVED/RETURNED` + 完整前后快照审计表 `InvalidFanReportAudit`)——这是历史客户补录审核（需求文档7.3）要抄的模板。`AuditLog`(通用日志)、`LeadActivity`(客户时间线，22种事件类型)、`LeadException`(导入异常)。

**设备**：`Device`(实体设备号) + `DeviceAccount`(聊天号码档案)——两者都是活外键，无历史表。

**专家8段流水线**：`ExpertWorkflowStage` enum = `QUEUED/MATERIALS/TRACKING/PENDING_REGISTRATION/PENDING_ORDER/DECLINED_DEPOSIT/ORDERED/STALLED`——**阶段1已验证**：[customer-workflow/mutations.ts:212-226](src/lib/customer-workflow/mutations.ts:212) 里 `markExpertStalled`/`markNoInitialDeposit` 的前置校验用 `registeredOn`/`customerOrder` 代理字段实现，效果上等价于"DECLINED_DEPOSIT 只能从 PENDING_ORDER 触发、STALLED 只能从 ORDERED 触发"，符合需求文档8.2语义，不需要改数据模型或服务层。唯一遗留：UI 文案仍是旧的"不愿充/杀不动"，没改成"未成交/停止维护"，留给阶段6前端接入时一并改。

---

## 认证与权限（现状）

- `auth.ts`：`requireUser()`/`requireRole(...roles)`，session-cookie，7天过期。
- 角色判断三种写法并存：页面内联 `if(user.role!==X)`、`role-access.ts` 的角色集合+辅助函数（`hasAssignedRole`/`hasAnyRole`）、`auth.ts` 的 `requireRole`。
- 记录级权限：`permissions.ts` 提供 `canWriteCustomerRevenue`/`canReadReportGroup`/`findLivePermissionUser`(写事务内重查活权限，防止会话过期期间越权)；多数路由仍是手写 Prisma where 片段（如 `customer-orders/route.ts` 的 `customerScope(user)`）。
- **阶段5a新增第四种写法**：[org-permissions.ts](src/lib/org-permissions.ts) 是只服务 `src/app/api/org/*` 新路由的独立权限网关，只读 `Duty`（+`companyId`/`departmentId`/`groupId`），完全不读 `Role` 字符串做层级判断（5.1 的 `canOperateCustomer` 例外，那条规则本来就是按 `Role` 定义的）；[org/_auth.ts](src/app/api/org/_auth.ts) 是仿 `admin/_auth.ts` 写的 `requireOrgManagerRequest()`，只确认"这是某一档组织管理者"，具体这一档能不能做某个动作交给 `org-permissions.ts` 的 `can*` 函数细判。这一层完全不接管老53个路由，两套权限网关并存，不是互相替代。

---

## API 路由清单（53个文件，按域分组）

- **auth/**：login、logout、change-password
- **leads/**：CRUD + check + downstream-progress（客户主流程）
- **customer-orders/**、**customer-finance/**：开单与资金
- **batches/**、**channels/**：批次与渠道（含广告花费）
- **admin/**：departments、groups、users(+transfer)、channels、settings、risk-*、audit-logs、customer-follow-up
- **company/**：department-managers、groups、leads（公司管理员视角，实际是老 `Role.COMPANY_MANAGER`=部门管理员视角，命名坑见下方阶段5a）
- **org/**（阶段5a新增，独立于上面53个老路由）：companies、departments、groups、groups/[groupId]/lead、structure（只读组织树）——总公司/公司/部门管理员三档新组织架构管理入口，权限网关是 `org-permissions.ts`，不是 `permissions.ts`/`role-access.ts`
- **lead/**：collaborations、conversion-standards、expert-assignments、members、performance-details（组长视角）
- **invalid-fan-reports/**：废号审核
- **group-customers/historical**、**expert-customers/historical**：历史客户补录（阶段6要统一成各岗位按负责阶段自助认领，并接上组长审核门禁）
- **exports/**：attendance、member-daily、member-performance 导出
- **notifications/**、**attendance/**、**daily-confirmations/**、**device-accounts/**、**events/**、**history/**：辅助功能
- **~~reports/~~**：已删（阶段0清理，零调用方，[7ea5bff](../../../../7ea5bff)）

---

## v2 原型 → 需求文档 映射（前端参考，冲突以需求文档为准）

| v2 原型概念 | 对应需求文档章节 | 已知分歧 |
|---|---|---|
| `DownstreamLead.expertStage`(8段) | 第八章 | v2 的"杀不动"沿用旧语义(需已开单)，需求文档8.2已重定义为"未成交/停止维护" |
| `CLAIM_BASELINE_STAGES`(炒群只能认领JOINED) | 7.3 | 一致 |
| 各岗位自助认领老客户 | 7.3 | 已与需求文档一致：各岗位认领自己负责的阶段，组长审核后正式生效 |
| `MoneyEvent`(首充/续充/出金统一数组) | 2.4 | 大体一致，真实后端是 `CustomerOrder`+`MetricEvent` 分离 |
| `FUNNEL_CHIPS` 当期比值口径 | 6.1 | 需求文档要同批口径，v2也是当期比值，两边都要改 |
| `RECEPTION_BY_GROUP_OPERATOR`(组长配对) | 1.4 | 一致 |
| 认领/录入时的"发生日期"字段 | 隐含在7.3的日期校验精神里 | 一致，是好的参考实现 |

---

## 重写进度

见 `/Users/aaaa/.claude/plans/merry-sauteeing-cook.md` 的"分阶段执行"一节。当前状态：阶段0、阶段1、阶段2、阶段3（Stage A + Stage B）、阶段4已完成，均已跨会话审查并修复到审查方给出"safe to build next phase on top of"结论（阶段0-3各一轮；阶段4经历五轮迭代审查，详见下方"阶段4"各小节）。阶段5a（后端：权限网关+组织结构路由，含管理员账号创建入口）已完成实现、独立验证并推送；阶段5b 已开始，第一切片是真实组织管理 UI，完成范围见本文后续记录。

**阶段1完成内容**：验证专家状态跳转（符合8.2语义，无需改动）+ 验证并修复无效库改判校验（与3.1.1冲突，已放开，见上）；新增 `Company`/`Position`/`Duty`/`UserPosition`/历史补录审核字段（纯 schema 新增）。SQLite 迁移 `20260828133105_add_company_position_duty_historical_review` 已应用到 `prisma/dev.db`；对应的 Postgres 迁移已手写并登记进 `checksums.json`（**未在真实 Postgres 实例上跑过 `prisma migrate dev` 生成，是照现有迁移风格手写的，接入生产库前建议先在测试库跑一遍 `db:postgres:test:prepare` 验证 SQL 语法**，阶段2的新迁移 `20260828140651_add_acting_position_and_secondary_positions` 同样未跑过真实库）。

**阶段2完成内容**：`recordMetricEvent`/`recordMetricEvents` 落地（`MetricEvent.actingPosition` 冻结字段），全仓库9处 `metricEvent.create`/`createMany` 已切换过去；`transferUserPosition` 落地（`UserPosition.secondaryPositions` 冻结字段），**从现有 `/api/admin/users/transfer` 路由整体重构而来**（不是平行新建，ADMIN/COMPANY_MANAGER 已在使用），保留原有全部校验/交接逻辑，新增岗位历史写入 + `User.duty` 维护 + 专家"未成交/停止维护"客户自动划给组长（见上）。新增单测覆盖：调岗后旧 MetricEvent 的 `actingPosition` 不随后续调岗改变（[metric-event-acting-position.test.ts](tests/unit/metric-event-acting-position.test.ts)）、调岗后 UserPosition 历史行正确关闭/开启、组长晋升不写 Position 行、放弃状态客户自动交接、真正在办客户仍要求手动指定接手人（[user-position-transfer.test.ts](tests/unit/user-position-transfer.test.ts)）。现有 `personnel-transfer.test.ts` 全部原样通过，确认重构未改变可观察行为。

⚠️ **阶段2跨会话审查发现并已修复**：首次实现里 `abandonedExpertWhere`（判定"放弃状态、自动交接给组长"）给 `DECLINED_DEPOSIT` 和 `STALLED` 共用了同一个 `customerOrder: null` 条件，但 `STALLED`（停止维护）按需求文档8.2定义只能在**已开单后**发生（`markExpertStalled` 强制要求有效 `customerOrder`，见 [mutations.ts:214](src/lib/customer-workflow/mutations.ts:214)），现实中 `customerOrder` 永远不是 `null`——导致 STALLED 客户转岗时静默不被交接给组长（请求仍返回200，`expertOwnerId` 却原样不变），是"看起来成功但没生效"的一类问题。原测试因为手写了一个绕过 `markExpertStalled` 前置校验的数据状态（STALLED 但没建 `CustomerOrder`）而误判通过。已修复：[transfer.ts](src/lib/user-position/transfer.ts) 把 `DECLINED_DEPOSIT`（要求 `customerOrder: null`）和 `STALLED`（不要求）拆成独立条件；测试fixture改为先建真实 `CustomerOrder` 再设置 `STALLED`，并补充"找不到组长时保持原样""`expertWorkflowStage` 为 null 仍算在办"两条用例。**教训**：给业务状态构造测试数据时，要按该状态在正常操作路径下的真实前置条件来builds，不能绕过状态机直接摆数据，否则测试会验证一个业务上不可达的场景。

⚠️ **阶段2遗留**：`Company`/历史补录审核字段仍无消费方（分别是阶段5、阶段6的工作）；`UserGroupMembership` 与 `UserPosition` 目前并存双写，尚未规划何时收敛成一张表。

**阶段3 Stage A完成内容**（需求文档4.1：系统不再计算渠道成本/定价/利润，净业绩=入金-出金是唯一留存的金钱指标；`channelType` 分类按需求文档4.2保留不动）：
- 删除孤儿页面 `reports/page.tsx`（阶段0已删其API，这次连页面本体一起删，第4份重复成本计算）和 `src/lib/analytics/canonical-financials.ts`。
- 改用 `netPerformanceCents` 重写了约20个 `src/lib/analytics/*`/`src/lib/boss-report/*` 文件（含 role-rankings/performance-leaderboard-query/channel-analysis/member-evidence/member-overview/overview/resource-workspace/company-workspace/headquarters-performance 等）和对应UI组件（`components/analytics/member/`、`components/analytics/overview/` 两个目录为主）。
- `finance.ts` 瘦身到只剩 `validateFanBreakdown`（跟钱无关，是 `batches/route.ts`/`history/route.ts` 的输入校验，不能删）。
- 三个渠道创建入口（`EntryReceptionPanels.tsx→channels.ts`、`ChannelManager.tsx→admin/channels/route.ts`、`expert-customers/historical/route.ts` 历史补录硬编码建渠道）已去掉价格/返点字段赋值，只留 `channelType`。
- **`ChannelManager.tsx`/`admin/channels/route.ts` 的编辑表单也一并去掉了价格/返点的编辑能力**（不只是创建入口）——这是执行中发现的必要延伸：Stage B 要删 schema 字段，如果编辑表单还能写这些字段，删字段那一步就会编译不过；同时旧的 `requiresChannelPriceClearConfirmation` 高危二次确认、`CHANNEL_PRICE_UPDATED` 审计动作也一并移除（ADMIN 介入渠道管理仍保留统一的"确认总公司介入渠道管理"二次确认，跟价格无关）。
- **额外发现并清理的死代码**（不在原计划清单里，是执行中顺带查出来的）：`canonical-events.ts` 的 `CanonicalMetricEvent.batch.channel` 里 `fanCostMode`/`effectiveFanPriceCents`/`rebateRateBps`/`channelType` 四个字段，下游9个消费方全部没读过；`member-daily-detail.ts` 的 `MemberDailyRow.costCents` 字段算了但 `MemberDailyDetail.tsx` 从没渲染过，且其代码注释还提到已被需求文档4.1整个删除的"计入业绩"概念——均已删除。
- 广告投流成本功能（`AdvertisingSpendEditor.tsx`/`advertising-spend`/`batches/advertising` 路由/`refreshAdvertisingBatchCost`）**故意未动**，是 Stage B 范围。
- ⚠️ **遗留给 Stage B 或后续会话**：`tests/e2e/admin-management.spec.ts` 里管理员渠道管理流程的 Playwright 用例还在点旧的价格表单/清除单价确认框（部分选择器如"所属小组"疑似在更早一次重构后就已经失效，不是这次改动直接导致的），已作为独立任务派发（spawn_task，未在本次会话内验证真实浏览器行为），不阻塞 `tsc`/`vitest` 主线。
- 验证：`npx tsc --noEmit` 干净，`npx vitest run` 全绿（162个测试文件、848 passed + 4 skipped，无失败；总用例数比阶段2末的863少是因为删除/合并了若干专测旧定价接口的用例）。

⚠️ **阶段3 Stage A跨会话审查（独立worktree agent）结果**：总体结论"safe to build on"，独立验证了 tsc/vitest 干净、高危二次确认门禁简化后权限语义不变、两处死代码删除确属死代码、被重写的测试确实还在测真实行为。发现并已修复1个真实bug + 记录1处行为变化：
  - **已修复**：[global-channels.ts](src/lib/global-channels.ts) 的 `copyGlobalChannelsToGroup`（新建小组时把全局渠道复制过去）漏选了 `channelType`/`rebateRateBps`，导致新小组里的全局渠道全部被 Prisma 默认值悄悄降级成 SMS——这是早于本阶段就存在的既存bug（本阶段两次成本字段清理时才被顺带查出来，不是这两次改动引入的），后果包括：`phone-import.ts` 客户编号前缀错误、`source-performance-summary.ts` 来源质量对比错误分类、投流批次流程在新小组里失效、以及一个资源部账号权限收窄绕过（`resourceChannelAccess` 只按 channelId 校验，type错误后可能放行不该有权限的渠道类型）。已在 select 里补上这两个字段。**Stage B删成本字段时如果不注意，这个 select 会自然收窄到只剩 channelType 相关字段，bug 会变得不明显——已确认新代码正确。**
  - **已记录（未改动行为）**：[data-risk.ts](src/lib/analytics/data-risk.ts) 的 `evaluateFinancialRisks` 触发门槛从"数据有效 && 已定价"改成了单纯"数据有效"——这是删成本字段的自然结果（`priceState`概念本身没了），效果是 `WITHDRAWAL_ANOMALY` 现在会在以前因为"渠道未定价"而被抑制的场景下也触发。审查认为这个改动本身是对的（出金异常检测应该是纯资金流判断，不该依赖定价），但补充记录一下，因为这确实是用户能感知到的告警行为变化，不只是内部重构。
  - **审查中一并顺手清理的遗留文案**（需求文档4.1"整个概念删除，不是隐藏"的收尾）：`role-rankings/page.tsx`、`EntryTabs.tsx`（2处）、`admin/_auth.ts`、`HistoricalCustomerDialog.tsx`、`HistoricalExpertCustomerDialog.tsx`、`lead-management-summary.ts` 里残留的"计入业绩/资源成本/单价"字样已改成中性表述或删除（注意 `EntryTabs.tsx` 里另外两处"计入业绩"字样是需求文档3.1.1无效库的正常业务概念，不是本次要删的成本口径，予以保留，没有动）。
  - **审查发现但判断可以留到后续处理**（已知，非本次范围）：导航"渠道与单价"标签（`app-navigation.ts`/`AppHeader.tsx`，被 `resource-channel-permissions.test.ts`/`management-command-center.test.ts` 两处测试钉住，改名需要联动测试，留作独立决定）；`RiskSettingsForm.tsx` 的 `priceComparisonMinOrders`（"单价比较最低开单数"）设置项——查证是**早于本阶段就没有消费方的既存死配置**，不是这次引入的，但词汇上明显违反需求文档4.1，值得单独清理；`MemberInsightDrawer.tsx`/`CompanyCommandCenter.tsx` 里两处不可达的 `"待定价"` 兜底文案（`financialFormula`/`netPerformanceCents` 实际从不为 null，走不到这个分支）。这两项已拆成独立任务派发（spawn_task），未阻塞本次推进。

**阶段3 Stage B完成内容**（删 schema 字段、finance.ts、投流广告费功能；已跨会话审查一轮，见下方"⚠️ 阶段3 Stage B跨会话审查"）：
- 删除 `finance.ts`：唯一函数 `validateFanBreakdown` 搬进 `validation.ts` 并导出，4 个调用方（`validation.ts` 内部、`member-evidence.ts`、`member-overview.ts`、`HistoryEditDrawer.tsx`）改导入路径。
- 删除投流广告费功能：`AdvertisingSpendEditor.tsx`/`AdvertisingBatchCreator.tsx` 组件、`batches/advertising`/`batches/[batchId]/advertising-spend` 两条路由、`db.ts` 的 `refreshAdvertisingBatchCost`。**同一渠道+日期多接粉员共享一个批次的机制本身保留**（`getOrCreateSourceBatch` 简化为纯批次身份判断，不再算成本），只是不再有人预先声明广告费、不再自动核算单粉成本。
- 删 schema 字段：`Channel.fanCostMode/effectiveFanPriceCents/rebateRateBps`、`SourceBatch` 对应三个快照字段 + 三个投流字段（`advertisingSpendCents/advertisingFanCount/advertisingServiceFeeRateBps`）、`FanCostMode` enum。SQLite 迁移 `20260828150000_remove_channel_cost_pricing_fields` 用 `prisma migrate diff --from-schema-datasource ... --to-schema-datamodel ...` 生成（`migrate dev` 因为是破坏性变更需要交互确认，非交互环境跑不了，改用 diff+`migrate deploy` 两步），已应用到 `prisma/dev.db`；Postgres 迁移逐字段手写核对（**同样未在真实 Postgres 实例跑过**），登记进 `checksums.json`（27→28）。
- 清理执行中发现的两个 schema 字段遗留消费方：`global-channels.ts` 的 `copyGlobalChannelsToGroup`（上面 Stage A 审查刚修过 `channelType` 丢失的 bug，这次顺带删掉了同一个 select 里的成本字段）、`legacy-customers/route.ts` 的历史客户补录批次创建。
- ⚠️ **踩坑记录，供以后类似改动参考**：`tsc --noEmit` 不能完全信任——`tests/unit/member-overview-query.test.ts` 的 `db` 变量显式标了 `let db: any`，`tests/unit/helpers/analytics-db.ts` 的 Prisma 调用参数经过 `.map()` 构造后再赋给 `data:`，两种写法都会让 TypeScript 的"多余属性检查"（excess property check）失效——字面量对象一旦先被存进一个宽松类型的变量，或者从一个没有显式返回类型标注的箭头函数里"路过"一趟，TS 就不再按字面量规则严格比对，只按结构兼容性宽松检查，多余字段就查不出来。这两处直到跑 `vitest` 才在运行时报错（`Unknown argument`）。**教训：删 schema 字段这类改动，`tsc` 干净只是必要条件，不是充分条件，必须让 `vitest` 完整跑一遍每个用到相关表的测试文件才算数**。
- 验证：`npx tsc --noEmit` 干净，`npx vitest run` 全绿（161个测试文件、845 passed + 4 skipped，跳过的2个是需要真实 Postgres 的集成测试，与本次改动无关）。

⚠️ **阶段3 Stage B跨会话审查（独立worktree agent）结果**：总体结论"safe to build on"，独立验证了 tsc/vitest 干净、两套 schema 字段完全一致、两份迁移删的列完全对应且能在真实 Postgres 上重放、`checksums.json` 28 条哈希全部核对无误、共享批次机制和 `channelType` 冻结均未受影响、`validateFanBreakdown` 搬迁逐字节一致。发现并已修复：
  - **纠正一处不实记录**：上面"踩坑记录"最初写了"本次额外做了一次不依赖 tsc、直接对 `src`/`tests`/`scripts` 全量 grep 字段名的收尾扫描，才彻底排查干净"——审查独立复查后发现**这句话不实，`scripts/` 目录当时根本没扫到**（`.mjs` 脚本既不在 `tsc` 覆盖范围也不在 `vitest` 覆盖范围，是这次"TS 多余属性检查失效"之外的第三个盲区：脚本文件本身连类型检查都没有）。已删除这句不实描述，改为记录真实排查范围；这也是为什么"全量扫描"这类描述以后要谨慎下结论——没有真正跑一遍才知道结果，不能只是"应该扫过了"。
  - **已修复的真实 runtime 崩溃**：6 个 `scripts/*.mjs` 引用了已删字段，3 个接了 npm script（`db:backfill:rebate`/`db:audit:snapshots`/`seed:local-demo`，后者是文档记录的本地开发种子脚本，新同事第一次跑就会崩）。`audit-batch-snapshots.mjs`/`backfill-rebate-channel-snapshots.mjs`/`seed-local-finance-report-demo.mjs` 三个整体就是围绕成本/返点/投流字段设计的，需求文档4.1下已无存在意义，直接删除（含 `package.json` 里 `db:backfill:rebate`/`db:audit:snapshots` 两条命令、README `## 11.1 历史底料渠道一次性回填`整节）；`seed-local-demo.mjs`/`seed-local-colour-demo.mjs`/`seed-local-company-groups-demo.mjs` 三个是通用本地演示数据脚本，只去掉了写成本字段的那几行，功能保留，三个都已实际跑过一遍确认不再报 `Unknown argument`。
  - **顺手清理的两处小项**：`admin-display.ts` 里 `classifyAdminFormError` 一条已经匹配不到任何真实表单字段的死分支（渠道价格表单早在 Stage A 就删了）；`tests/unit/batch-key.test.ts` 的"多接粉员共享批次"用例原来建了两个接粉员账号但从没真正用来写数据，跟同文件第一条用例实质重复，改成真的让两个账号各写一条 `metricEvent` 到同一批次再断言两者都在。
  - **审查发现但判断可以留到后续处理**（已知，非本次范围）：`prisma/sqlite/schema.prisma`（阶段1起就标注为"过时分叉，别照它改"的那份）连累 `db:postgres:test:prepare`/`scripts/migrate-sqlite-to-postgres.mjs` 这条 Postgres 上线演练链路——本来就因为缺 `Company`/`UserPosition` 等阶段1-2新表而过时，现在又多缺了这次删除的字段（虽然是"多删"不是"少删"，方向相反，但同样会在跑这条链路时炸）；`admin-display.ts`/`ChannelManager.tsx`/`EntryReceptionPanels.tsx`/`resource-channels/page.tsx` 里"共享批次"文案仍然写成"投流粉专属"，但机制本身现在对所有渠道类型一视同仁——这是文案是否要跟着放开的产品判断，不是技术bug，留给以后有业务确认时再改。
- 验证（审查修复后重跑）：`npx tsc --noEmit` 干净，`npx vitest run` 全绿（161个测试文件、845 passed + 4 skipped）；三个修复后的 `.mjs` 脚本已实际执行验证不报错。
- 阶段3（Stage A + Stage B）至此全部完成并通过两轮跨会话审查，可以推进阶段4（同批口径分析重写）。

**阶段4完成内容**（需求文档6.1"同批口径"、6.1.1"当前在群是快照"；已跨会话审查五轮，最后一轮结论"safe to build phase 5 on top of"，见下方各小节）：
- ⚠️ **开工前摸底后，计划文档写的范围整个是错的**（跟阶段3渠道成本清理那次一样的教训）：`role-rankings.ts`/`performance-leaderboard-query.ts` 本来就已经是同批口径了（分子分母分离干净，`happenedBy(value, today)` 无下限，现有 `boss-role-ranking-asof.test.ts` 已经证明），不需要重写，计划里说要新建的 `loadCohortFunnel` 没有建。真正要修的范围小很多，但性质不完全一样，拆成两块：
  1. **需求文档6.1.1"当前在群"快照**：核实过实际读这个字段的地方，真正有问题的只有3处（`role-rankings.ts` 的 `currentInGroup`、`channel-analysis.ts`→`ChannelQualityTable.tsx`、`src/lib/metrics.ts` 的 `calculateBatchTotals`），人群都被报表选中的 `sourceDateFrom`/`sourceDateTo` 卡住了——选宽范围和选窄范围，"当前在群"的答案本该一样，实际不一样。~~`personal-data/page.tsx`/`EntryOverview.tsx`/`customer-queries/group-customers.ts` 看着像但其实用的是活字段 `groupStatus`，本来就没有日期范围限制，不用动~~（**已纠正，见下方跨会话审查发现**：`group-customers.ts` 的 `loadGroupPerformanceSummary` 其实也中招，`personal-data/page.tsx` 的 `currentInGroup` 函数本身没问题）；d7/d14"成熟期"展示（`getMaturity`/`MaturityWindowCards`）是另一个独立合法功能，不是6.2要删的东西，也不用动。修法：`BatchTotals.inGroup` 整个删掉（这个通用聚合函数只能看到调用方传入的、已经按范围筛过的数组，天生做不出不受范围影响的快照，继续留着只会不断在新调用方身上重犯这个bug）；`role-rankings.ts`/`channel-analysis.ts` 各自新增一个不带 `sourceDate` 过滤、只按 `groupId`/`channelId`/`memberId` 筛选、按 `joinedOn <= today` 且未退群计算的独立快照查询，只在这两处真正需要的地方接。新增回归测试锁定"选宽范围和选窄范围，当前在群答案必须一样"这个此前完全没测过的场景（[boss-role-ranking-asof.test.ts](tests/unit/boss-role-ranking-asof.test.ts)、[channel-analysis-query.test.ts](tests/unit/channel-analysis-query.test.ts)）。
  2. **需求文档6.1本身的残留违规——`member-daily-detail.ts`/`MemberDailyDetail.tsx`（组员每日明细表）的六个比率列**：这张表按"事件发生日"给每个指标分别归类到不同的行（比如添加按 `batch.sourceDate` 归到"添加日"，回复按 `repliedOn` 归到"回复发生日"），六个比率列（回复率、进群率、异常退群率、第3天推专家率、注册率、开单率）全部拿"某天发生了X次"除以"某天发生了Y次"，是需求文档6.1开篇反面教材本身。**怎么修是产品决定，不是纯技术判断**——已经跟用户确认（AskUserQuestion）：不改成同批口径（那样会改变"开单"/"当天推专家"这些原始计数的含义，牵连Excel导出的实际数字），而是**保留当天活动日志的原始语义，把六个比率列整个删掉**，因为按当天活动语义包装成"XX率"百分比本身就是误导，删比率不删原始计数。顺带删除了从没被展示过的 `abnormalLeft` 死字段（唯一用途就是那条被删的"异常退群率"）。
- 验证：`npx tsc --noEmit` 干净，`npx vitest run` 全绿（161个测试文件、847 passed + 4 skipped）。

⚠️ **阶段4跨会话审查（独立worktree agent，opus）结果**：结论"not safe to build phase 5 on top of as-is"，发现一处阻断级回归 + 若干真实bug，均已修复：
  - **阻断级回归**：`role-rankings.ts` 新增的 `loadCurrentInGroupByOperator`（后改名 `loadUnboundedInGroupLeadsForOperator`）是自成一体的"查询+计数"函数，写的时候忘了这段快照逻辑原本依赖主循环那套三层归属兜底（明确指派 → 最近一次推专家动作 → 当前配对组长），导致没有明确指派、靠"配对组长"兜底吃到客户的组长，`currentInGroup` 被静默清零。**修法**：改成先查原始 lead 数据，等 `currentOperatorByReception` 这个配对表建好之后再套用完全相同的三层判定逻辑；新增回归测试专门验证"没有明确指派、靠配对兜底"这条路径（[boss-role-ranking-asof.test.ts](tests/unit/boss-role-ranking-asof.test.ts)）。
  - **自己这次改动引入的新bug**：`member-performance-xlsx.ts`/`member-performance-report.ts` 的组长导出行原来拿 `currentInGroup` 当"进群"塞进跟"异常退群率"共用分母的位置——这在 `currentInGroup` 还是旧的、被范围卡住的语义时凑巧算对，但这次把 `currentInGroup` 改成真快照后，就变成了拿"不受范围限制的全量在群数"和"这个范围内的退群动作数"硬凑比率，反而制造了一个新的"分子分母不同批人"违规，正是阶段4本该消灭的反面模式。**修法**：改用 `sharedCustomerCount`（跟异常退群率原本就同一批人，`RoleRankingsTable.tsx` 里早就这么用）。
  - **计划里说要做但commit 2漏做的**：`member-daily-detail.ts` 的 `row.inGroup`/`row.eligibleForExpert` 当时只删了六个比率列，population 本身还是被 `leads`（按 `[from,to]` 筛过）卡住的，没有真正做成不受范围限制的快照。**修法**：新增 `inGroupLeads` 查询（只按 `groupOperatorOwnerId`/`groupId`/`channel` 过滤，不卡 `joinedOn` 范围），`row.inGroup`/`row.eligibleForExpert` 改用它按 `joinedOn <= 当天 && (!leftOn || leftOn > 当天)` 计算；新增回归测试。
  - **`customer-queries/group-customers.ts` 的 `loadGroupPerformanceSummary`**：`inGroup` 这个子聚合跟 `handled`/`introduced`/`left` 等字段共用一条 SQL、共用一个 `sourceDateFilter`——其它字段共用同批口径没问题（本来就该是"这个范围内经手的这批人现在怎么样了"），唯独 `inGroup` 不该被 `sourceDateFilter` 卡（页面默认"全部"不选日期，所以只有用户手动缩小范围才会踩到，严重度较低但确实是真bug）。**修法**：新增 `loadCurrentInGroupByOperator` 独立查询（不带 `sourceDateFilter`，保留 `queryFilter`），只在 `input.sourceDate` 存在时才跑这条查询去覆盖 `inGroup` 字段，其余字段不动；新增回归测试（[group-customer-in-group-snapshot.test.ts](tests/unit/group-customer-in-group-snapshot.test.ts)）。
  - **`personal-data/page.tsx`（前台个人数据页）遗漏的同一bug**：commit 2 的说法"member-daily-detail.ts 是最后一处6.1残留违规"不实——这个页面（接粉/炒群/专家三个角色天天看的个人仪表盘）有同样"分子分母不同批人"的问题，而且范围比 member-daily-detail.ts 更大：不只是每日明细表，连区间汇总里的比率也中招（组长"推专家率"、专家"联系率/注册率/开单率"），一共10处。跟用户确认过（AskUserQuestion）处理方式：**区间汇总的比率改成真同批口径，每日明细表的比率直接删**（跟 member-daily-detail.ts 一样，单独一天的分子分母本来就凑不成一批人）。新增 `operatorCohortFunnel`/`expertCohortFunnel`（对应组长按"进群落在选中区间"、专家按"推专家落在选中区间"圈定同一批客户，再看这批人里有多少在区间截止日前走到了下一步），替换掉原来各自独立按日期筛选后相除的写法；两个函数已导出并有纯函数单测（[personal-data-cohort.test.ts](tests/unit/personal-data-cohort.test.ts)）。
  - **两处次要发现**：`channel-analysis.ts` 的 `loadCurrentInGroupByChannel` 用 `ownerId: scope.memberId` 筛人，跟同文件 `loadCanonicalMetricEvents` 用"粉的归属 `attributionOwnerId` 优先、`ownerId` 兜底"的口径不一致——已改成一致写法；`channel-analysis-query.test.ts` 新增的"当前在群不受范围卡人群"用例原来只查了窄范围一次，没有真正对比宽范围，起不到锁定"范围不变答案"这个断言的作用——已加上宽范围对比查询。
- 验证（审查修复后重跑）：`npx tsc --noEmit` 干净，`npx vitest run` 全绿（163个测试文件、852 passed + 4 skipped）。

⚠️ **阶段4第二轮跨会话审查（独立worktree agent，opus）结果**：结论"not safe to build phase 5 on top of as-is"——比上一轮好很多（7项发现里5项确认修复干净：阻断级回归、member-daily-detail.ts、personal-data.tsx、channel-analysis.ts归属口径、channel-analysis-query.test.ts），但发现一处仍未修完 + 一处这次改动自己带来的新问题：
  - **`customer-queries/group-customers.ts` 只修了一半**：`loadCurrentInGroupByOperator` 这条不带 `sourceDateFilter` 的查询本身没问题，但覆盖逻辑只对"筛完范围后本来就有一行"的操作员生效——如果一个操作员在选中的窄范围里一条经手记录都没有（比如客户是很早以前进的群，选中范围内完全没有任何按 `sourceDateFilter` 能匹配到的活动），那这个操作员在筛过范围的查询结果里根本不会出现一行，`unboundedInGroup` 这张表就轮不到他，页面上直接显示成0——这正是6.1.1要消灭的"换个范围答案就变"。审查用真实探针数据复现：宽范围显示1，窄范围显示0。**修法**：把 `summaries`（范围过滤后的行）和 `unboundedInGroup`（不受范围限制的快照）按 operatorId 取并集——已有行的直接覆盖 `inGroup`；只在快照里出现、范围过滤后完全没有行的操作员，单独补一行（除 `inGroup` 外全部字段填0）。新增回归测试专门覆盖"窄范围内零经手记录"这个此前完全没测到的场景（[group-customer-in-group-snapshot.test.ts](tests/unit/group-customer-in-group-snapshot.test.ts)），并且验证过这条测试在修复前确实会失败（跑了一遍 `git stash` 复现）。
  - **本次改动自己引入的新问题**：`member-performance-xlsx.ts`/`member-performance-report.ts` 组长导出行改用 `sharedCustomerCount` 填"进群"这一栏后，比率本身是对的（分子分母同批人），但"进群"这个表头对炒群来说本来就没有对应的原始概念——`sharedCustomerCount` 实际是"接手客户数"，被塞进"进群"表头下显示会误导看报表的老板/管理员，跟这轮改动想消灭的"数字看着像别的东西"是同一类问题，只是换了个位置。**修法**：炒群行"进群"这一栏改回留空（`xlsx.ts` 里 `joined: null`，`report.ts` 里 `joined: "—"`，效果对齐专家行本来就有的留空模式）；异常退群率的分母单独拆出一个 `abnormalLeaveRateBase` 字段（xlsx.ts 的 `metricValues()` 原来直接拿共享的 `row.joined` 当分母，不能再借用一个现在要留空的字段）。新增两个导出模块此前完全没有的炒群行单测（[member-performance-report.test.ts](tests/unit/member-performance-report.test.ts)、[member-performance-xlsx.test.ts](tests/unit/member-performance-xlsx.test.ts)，后者顺带把 `operatorRow`/`metricValues` 导出以便直接测）。
  - **记录但判断可以留到后续处理（非本次范围）**：`role-rankings.ts` 的 `loadUnboundedInGroupLeadsForOperator` 比阶段4第一版多查了 `activities` 关联、也不能再按 `groupOperatorOwnerId: { not: null }` 收窄（三层归属兜底要求必须查出所有"未退群"的候选，不能在SQL层先按有没有明确指派过滤掉，否则又会复现这轮修的阻断级回归）——这是正确修复的必然代价，不是可以顺手改掉的bug。真正的隐患在 `member-performance-export.ts` 按天循环调用 `loadRoleRankings`（月报约30次并发），每次都会重新跑一遍这条不便宜的全量查询，只有 `today` 这个截止日不同——SQLite测试库上看不出来，真上生产 Postgres 量级大了可能是个真实性能问题。技术方向：把这条查询的"拉取"和"按天截断计算"拆开，一次性拉够数据后在内存里按不同 `today` 切片，而不是每天重新查一遍库；这需要动 `member-performance-export.ts` 的调用结构，留给以后专门优化，不阻塞阶段5。
- 验证（第二轮修复后重跑）：`npx tsc --noEmit` 干净，`npx vitest run` 全绿（166个测试文件、856 passed + 4 skipped）。

⚠️ **阶段4第三轮跨会话审查（独立worktree agent，opus）结果**：`group-customers.ts`/导出列这两处修复本身确认干净（独立复现过"改前失败、改后通过"），但发现了同一类问题在另一处没堵上，另有一处本轮改动自己写的注释说过头了：
  - **`channel-analysis.ts` 有一模一样的行缺失问题**：`rows` 是从 `grouped`（按选中范围内的 `events`/`imports`/`approvedInvalidReports` 建出来的 Map）遍历生成的——如果一个渠道选中范围内一条动态都没有，但手里还有没退群的老客户，这个渠道在 `grouped` 里根本不会有条目，整行直接消失，答案照样随日期范围时隐时现。审查用探针复现：宽范围能看到这一行，窄范围整行没有。**修法**：`loadCurrentInGroupByChannel` 改成连渠道展示名和所属小组一起返回（原来只返回计数），这样才能在只有快照、没有任何范围内动态的情况下独立拼出一整行；`rows` 的生成从纯 `grouped.entries().map()` 改成先算 `groupedRows`，再对 `currentInGroupByChannel` 里在 `grouped` 找不到的 key 单独补一行（`newFans`/`submitted`/`effective` 等全部字段填0，`rankable` 恒为 false），最后合并排序。新增回归测试锁定这个此前完全没测到的场景（[channel-analysis-query.test.ts](tests/unit/channel-analysis-query.test.ts)），验证过修复前会失败。
  - **`group-customers/page.tsx` 的"未分配炒群岗"分支同一个坑**：`loadGroupPerformanceSummary` 侧的并集修复是对的，但页面侧 `if (unassignedSummary?.handled)` 这个门槛只看 `handled`（选中范围内有没有经手记录）——范围内零经手记录、但手里还有没退群老客户时 `handled` 是0，这一行照样会跟着范围消失。**修法**：把判断逻辑抽成 `shouldShowUnassignedRow`（导出的纯函数，用类型谓词让 TS 能在调用处正确收窄），改成"`handled` 或 `inGroup` 有一个非零就展示"；新增单测直接测这个函数（[group-customers-unassigned-row.test.ts](tests/unit/group-customers-unassigned-row.test.ts)）。已顺带自查了其它同样"遍历 Map 生成行列表"的地方（`role-rankings.ts` 的 `groupOperators` 映射是遍历真实花名册 `people` 再按 id 查表，不会漏；`member-daily-detail.ts`/`personal-data/page.tsx` 都是单个成员，不涉及"遍历多行"这个维度），确认这类bug目前只出在这两处，已全部堵上。
  - **本轮改动自己的注释说过头了**：`member-performance-report.ts`/`member-performance-xlsx.ts` 里新写的注释说异常退群率"分子分母同一批人"——但 `role-rankings.ts` 里 `abnormalLeaveActions`（按谁点了退群操作算）和 `sharedCustomerCount`（按客户归属的三层兜底算）本来就是两条不同的归属轴线，同事代班处理退群时两者不保证是同一批人，理论上比率能超过100%。这是 `role-rankings.ts` 自初始提交就有的既有口径特性，不是这轮改动引入的，也不在这轮修复范围内（牵连主榜单的归属模型，改起来更大）——已改正两处注释的措辞，不再声称"保证同批人"；已记录为独立待办（`task_b36b909b`，等用户决定要不要在后续阶段处理）。
  - **顺带发现但非本轮范围的死代码**：`member-performance-report.ts` 的 `memberPerformanceReportRows`/`memberPerformanceCsv` 在 `src/` 里没有任何真实调用方，只有它自己的测试引用——已记录为独立待办（`task_39c24d0f`），比照提交 `7ea5bff`（删除孤立的旧报表组件 BatchReportTable）的先例，等确认后删除。**2026-08-29更新：已确认删除**（`src/lib/member-performance-report.ts` + `tests/unit/member-performance-report.test.ts`），删除前重新确认过全仓库无引用，`npx tsc --noEmit` 干净，`npx vitest run` 全绿（168个测试文件、924 passed + 4 skipped）。上面几条提到的 `member-performance-report.ts` 均为历史记录，该文件现已不存在。
- 验证（第三轮修复后重跑）：`npx tsc --noEmit` 干净，`npx vitest run` 全绿（165个测试文件、861 passed + 4 skipped）。

⚠️ **阶段4第四轮跨会话审查（独立worktree agent，opus）结果**：`channel-analysis.ts`/`group-customers/page.tsx` 这两处修复本身确认干净（独立复现过"改前失败、改后通过"），但发现同一类问题的**第4处，而且是最重要的一处**——就出在role-rankings.ts主榜单本身，外加一处"未分配专家"分支的同款问题，以及上一轮代码地图"这类bug已全部堵上"的说法被打脸：
  - **BLOCKING：`role-rankings.ts` 的 `groupOperators` 花名册筛选本身漏了快照**：`people.filter(person => hasScopedRole(person, "GROUP_OPERATOR") || leadsByOperator.has(person.id))`——`currentInGroupByOperator`（不受范围影响的在群快照）压根没进这个筛选条件。如果一个人既不是组长角色、窄范围内又没有任何经手记录（比如靠"最近一次推专家动作"这层兜底被快照归属到、但本人是专家不是组长），这个人会连行都进不了 `groupOperators` 花名册，他名下的在群数直接凭空消失——这正是阶段4要消灭的问题本身，而且出在这轮review的起点文件、还会传导到老板简报。**修法**：筛选条件加上 `|| currentInGroupByOperator.has(person.id)`；因为 `groupOperators` 本来就是遍历真实花名册 `people` 再按 id 查表（不是像 `channel-analysis.ts`/`group-customers.ts` 那样直接遍历 Map 的 key），所以不需要额外拼合成的行，只要把这个人放进筛选结果，后面 `currentInGroup: currentInGroupByOperator.get(person.id) ?? 0` 自然就对了。新增回归测试锁定这个此前完全没测到的场景（[boss-role-ranking-asof.test.ts](tests/unit/boss-role-ranking-asof.test.ts)），验证过修复前会失败。
  - **`expert-customers/page.tsx` 的"未分配专家"分支同款问题**：`if (unassignedSummary?.handled)` 只看 `handled`，但 `pendingRegistration`/`pendingOrder` 这两个字段没有跟 `handled` 一样的历史补录口径判断，一个客户完全可能 `handled` 记成0、但仍然挂着真实的待注册/待开单——这一行会跟着范围/统计口径消失，而这一行恰恰是按待办数量排序的。**修法**：照group-customers/page.tsx的模式抽出 `shouldShowUnassignedExpertRow`（"handled/registered/ordered/pendingRegistration/pendingOrder 任一非零即展示"），新增单测（[expert-customers-unassigned-row.test.ts](tests/unit/expert-customers-unassigned-row.test.ts)）。审查顺带指出 `pendingRegistration`/`pendingOrder` 本身在 SQL 里完全没有历史补录的口径闸门（`handled` 有，这两个没有），这是比展示门槛更深一层的问题，且这两个字段目前仍然被可选的 `sourceDateFilter` 卡着——待办和 `group-customers.ts` 的 `inGroup` 是不是该用一样的思路解开还需要先去核对需求文档，已记录为独立待办（`task_da86994c`），不在本轮范围内。
  - **顺手把 `shouldShowUnassignedRow` 的类型谓词改回普通布尔函数**：审查指出原来 `summary is T` 这个类型谓词在 false 分支会把"存在但字段全是0的对象"错误收窄成 `undefined`，虽然当前没有 `else` 分支不会出错，但是给下一个编辑者埋了个坑。改成 `shouldShowUnassignedRow(summary: T): boolean`（不接受 `undefined`），调用处改成 `if (unassignedSummary && shouldShowUnassignedRow(unassignedSummary))`——用 `&&` 短路本身做收窄，不需要自定义类型谓词。`shouldShowUnassignedExpertRow` 一开始就按这个更安全的写法实现。
  - **纠正上一轮代码地图的过头断言**：上一轮写的"已顺带自查了其它同样'遍历 Map 生成行列表'的地方…确认这类bug目前只出在这两处，已全部堵上"——这句话被审查证伪了，`role-rankings.ts` 的 `groupOperators` 花名册筛选不是"遍历 Map 生成行列表"（是遍历真实花名册），但依然会漏，说明自查范围想得不够全，只看了"生成行列表的方式"这一个维度，没检查"筛选条件本身有没有覆盖到快照"这个更本质的问题。这次不再断言"已全部堵上"，只记录"目前已知的这4处都已修复，不代表排查穷尽"。
- 验证（第四轮修复后重跑）：`npx tsc --noEmit` 干净，`npx vitest run` 全绿（166个测试文件、864 passed + 4 skipped），`npm run build` 成功。

✅ **阶段4第五轮跨会话审查（独立worktree agent，opus）结果："safe to build phase 5 on top of"，本轮review周期结束**：
  - 两处修复均确认干净——`role-rankings.ts` 的 fix 独立复现过"改前失败、改后通过"（`git show c688090^` 版本跑同一条新测试确实失败）；`shouldShowUnassignedRow`/`shouldShowUnassignedExpertRow` 的类型谓词改动也确认 `tsc` 无收窄报错。
  - **对"这类bug到底堵完没有"做了一次真正系统的排查**：逐一列出全仓库当前所有"不受范围影响的快照/独立查询"（`role-rankings.ts` 的 `loadUnboundedInGroupLeadsForOperator`、`group-customers.ts` 的 `loadCurrentInGroupByOperator`、`channel-analysis.ts` 的 `loadCurrentInGroupByChannel`、`member-daily-detail.ts` 的 `inGroupLeads`、`personal-data/page.tsx` 的 `currentInGroup`），确认每一处的"消费方"要么是按真实花名册/固定日期集合遍历（安全），要么本身就没有范围过滤（安全）——**没有找到第5处同类问题**。`member-daily-detail.ts`（按 `allDates(from,to)` 预先建好每天的行，不是遍历 Map 的 key）、`personal-data/page.tsx`（查询本身完全没有日期过滤）结构上天然不会中招。
  - `boss-report/data.ts` 单独核实过：它确实会对 `loadRoleRankings` 的 `groupOperators` 结果再筛一遍（`row.active && row.eligibleForIntroduction > 0`），快照归属但样本不足的操作员会被排除——判断这是合理的（老板简报这里要展示的是"第3天推专家率"评级，分母为0没法评级；同样的"有没有样本"闸门在接粉/专家两边也一样用；老板简报的统计窗口是固定滚动30天，不是用户可选范围，不存在"换个范围答案就变"的暴露面），不是bug复发。
  - **新发现（非阻断，已记录为独立待办 `task_b36b909b` 的同类问题）**：`member-daily-detail.ts` 的组长客户归属只看 `groupOperatorOwnerId`（`ownerWhere`/`inGroupLeads` 两处），不像 `role-rankings.ts` 用三层兜底——同一个操作员在"每日明细"里的当日在群数可能比"主榜单"的当前在群数偏低（配对兜底/最近推专家兜底归到的客户，在明细里查不到）。两个视图各自内部自洽，是归属轴线不一致，不是这轮修的"行缺失"这一类问题。
  - ⚠️ **纠正上一条**：这里当时判断"目前'异常排行'页面的成员钻取明细尚未接入调用方，暴露面有限"——**2026-08-29 前端核对后证明这个判断是错的**。`loadMemberDailyDetail` 的唯一调用方 `anomaly-ranking/page.tsx` 早就接好了：页面的"炒群成员"标签页（`MemberOverviewTabs.tsx`）+ 成员筛选下拉框，组长/管理员选中一个炒群成员就能看到这张明细表，ADMIN/RESOURCE_MANAGER/COMPANY_MANAGER/LEAD 都能到达。也就是说组长今天登录系统、点开自己团队某个炒群成员的每日明细，看到的"当日在群/当天退群"数字，跟同一时间段主榜单/老板简报给这个操作员算出来的数字，就可能真的对不上——不是理论上的隐患，是当前就能被用户看到的真实问题。已把待办任务（`task_cead2050`）换成标注了正确暴露范围的新任务（`task_2a2e556e`）。
  - ✅ **每日明细归属已修复（2026-08-29）**：新增 [group-operator-attribution.ts](src/lib/group-operator-attribution.ts) 作为炒群归属唯一判断，`role-rankings.ts` 与 `member-daily-detail.ts` 共同使用“明确指派 → 截止日前最近一次推专家动作 → 当前接粉配对”三层规则。每日明细查询不再在数据库入口直接排除没有明确指派的客户；先取可能候选，再用同一函数精确归属。回归测试同时覆盖三层优先级，并核对主榜单与每日明细的当日在群数一致。
- 验证（第五轮）：`npx tsc --noEmit` 干净，`npx vitest run` 全绿（166个测试文件、864 passed + 4 skipped），`npm run build` 成功。
- **阶段4至此结束，五轮跨会话审查全部完成，可以推进阶段5。**

**阶段5a完成内容**（需求文档5.1/5.2/5.6：组织架构操作权限的新权限网关+新路由，纯后端，不含UI；命名坑决策见 `/Users/aaaa/.claude/plans/merry-sauteeing-cook.md` 阶段5小节，本次按该决策执行，未重新讨论）：

- **Schema**：`User` 新增可空 `companyId`（FK→`Company`，`onDelete: SetNull`，同时给 `User`/`Company` 都加了索引/反向关系，镜像 `departmentId`/`Department.managers` 的既有写法），只给 `Duty.COMPANY_MANAGER` 账号用；`Duty.HQ_MANAGER` 不需要范围字段。SQLite 迁移 `20260829030000_add_user_company_id` 用 `prisma migrate dev` 生成并应用到 `prisma/dev.db`（纯新增可空字段，非交互环境能直接跑，不需要 Stage B 那种 diff+deploy 两步走）；Postgres 迁移手写核对，登记进 `checksums.json`（28→29，**同样未在真实 Postgres 实例上跑过**，跟阶段1/2/3一致）。
  - ⚠️ **踩坑记录**：第一次忘了在 schema 里给 `companyId` 加 `@@index`，导致 `migrate dev` 先后生成了两个migration（加字段、加索引各一个）。发现后把两份 SQL 合并进一个migration文件夹，但这时候 `prisma/dev.db` 已经把两条迁移记录都写进了 `_prisma_migrations` 表——Prisma CLI 检测到调用方是 agent，直接拒绝了 `prisma migrate reset`（"forbidden without explicit consent"的安全门禁），没有去申请用户批准来绕过，而是改用 `sqlite3` 直接、非破坏性地修正 `_prisma_migrations` 表记录（删掉两条旧记录，插入一条对应合并后migration文件名的新记录，checksum 用 `shasum -a 256` 现算，跟 Prisma 自己算的算法一致），`prisma migrate status` 复核确认"up to date"。记录这个是因为以后如果再遇到"本地误生成了多余的中间迁移、想squash成一个干净的"，`migrate reset` 这条路在agent环境里是走不通的（且本来也不该在有真实数据的库上用），直接改 `_prisma_migrations` 表是更安全的替代方案。

- **一次性数据回填**：[scripts/backfill-department-manager-duty.mjs](scripts/backfill-department-manager-duty.mjs)——把现存的 `Role.COMPANY_MANAGER` 账号（`duty` 为空的）一次性补上 `Duty.DEPARTMENT_MANAGER`。核心查询/更新逻辑抽进 [backfill-department-manager-duty-lib.mjs](scripts/backfill-department-manager-duty-lib.mjs)（仿照 `rotate-initial-account-passwords-lib.mjs` 的拆分方式，单测直接传假 db 对象验证，不需要真的起数据库），CLI 脚本只管真实连接、预览打印、`CONFIRM_DEPARTMENT_MANAGER_DUTY_BACKFILL=YES` 确认门禁（仿照已删除的 `backfill-rebate-channel-snapshots.mjs` 的预览+确认模式）。已针对 `prisma/dev.db` 实际跑过：找到2个 `Role.COMPANY_MANAGER` 账号（`company`/`demo_company`），`duty` 均为空，回填后都变成 `DEPARTMENT_MANAGER`；再跑一次确认幂等（0个账号待回填，未报错、未重复写入）。
  - ⚠️ **偏离计划文档字面表述的地方**：计划要求"仿照 scripts/ops/ 的既有惯例"，但 `scripts/ops/` 在本文档里明确标注是"只读运维脚本（对账、审计）"，这次要做的是真实的一次性写库操作，硬塞进 `scripts/ops/` 会破坏那个目录本身的既有约定。改为放在 `scripts/` 根目录，跟同样是"写库"性质、已删除的 `backfill-rebate-channel-snapshots.mjs`（也在根目录，不在 `ops/` 下）保持同一惯例——读原则而非抠字面。

- **新权限网关**：[src/lib/org-permissions.ts](src/lib/org-permissions.ts)，实现需求文档5.1（`canOperateCustomer`）、5.2（`canViewOrgScope`）、5.6（`canCreateCompany`/`canCreateDepartment`/`canCreateGroup`/`canAppointOrTransferLead`）。均为不查库的纯函数，参数是调用方已经解析好的范围对象（跟 `permissions.ts` 的 `canReadReportGroup` 接收已解析的 `group` 对象是同一种写法），不是接一个裸 id 在函数内部现查库——`canCreateDepartment`/`canCreateCompany` 因此也简化成不接收目标参数（计划建议的 `canCreateDepartment(user, companyId)` 里 `companyId` 对权限判断本身没有影响，永远只看 `Duty.HQ_MANAGER`，公司是否存在/启用是路由自己的数据校验，不属于权限判断）。`canOperateCustomer`（5.1）本次没有任何新路由消费它——5a范围只有组织结构操作，不涉及客户操作路由，这是计划文档建议清单里的函数，为未来阶段（客户操作类新路由）预留，不是死代码。

- **新路由**（均在 `src/app/api/org/` 下，独立于老53个路由，用 `org/_auth.ts` 的 `requireOrgManagerRequest()` 统一登录闸门+各自的 `can*` 函数细判）：
  - `POST /api/org/companies`：建公司，仅 `Duty.HQ_MANAGER`。
  - `POST /api/org/departments`：在某公司下建部门（含国家/时区，复用 `Department.countryCode`/`timezone`），仅 `Duty.HQ_MANAGER`。跟老的 `admin/departments/route.ts` 不是同一条路由，那条是 ADMIN 专用、不挂 `companyId` 的老入口，两者并存，互不影响。
  - `POST /api/org/groups`：在某部门下开新组，按层级限定（部门管理员限本部门/公司管理员限本公司/总公司不限）。复用 `copyGlobalChannelsToGroup` 保持渠道初始化行为跟老路由一致；**新组时区固定继承所属部门**，不像老的 `admin/groups/route.ts` 那样允许小组单独覆盖时区——这是刻意收紧，因为需求文档1.2"组不能有自己独立的时区"，老路由的覆盖能力是两层结构时代留下的口子，新的四层结构入口不应该重开这个口子（老路由本身不在本次范围内，未改动）。
  - `POST /api/org/groups/[groupId]/lead`：任免/调动一个小组的组长，权限判断通过后**调用阶段2的 `transferUserPosition`**（固定 `role: "LEAD"`、`secondaryRoles: []`，不重新发明转岗逻辑）。
  - `GET /api/org/structure`：公司→部门→小组的只读 drill-down 树，按 `Duty` 返回不同粒度（HQ看全部公司分开列、公司管理员看自己公司、部门管理员看自己部门），5.5"不合并"体现在返回形状本身（HQ拿到的是 `companies` 数组，不是一个合并对象）；额外加了 `unassignedDepartments`桶专门装 `companyId` 为空的存量部门（见上方"组织架构"一节），否则这批部门会在HQ视图里凭空消失。

- **两处计划文档字面表述之外、需要自己判断的设计决策**（跟阶段3/4惯例一样，记录下来避免以后看代码看不懂为什么这么写）：
  1. **`canAppointOrTransferLead` 只表达了"目标小组"这一条规则**（计划文档原文如此），但公司管理员/部门管理员的管辖范围本来就是"本公司内"/"本部门内"——只查目标一端会漏掉"把人从别的公司/部门直接调过来"这个越权口子（HQ不受此限，本来就该不限）。`groups/[groupId]/lead/route.ts` 里对调出的原小组和调入的目标小组都跑了一遍**同一个** `canAppointOrTransferLead` 函数，两边都通过才放行——没有新增判断逻辑，只是同一个纯函数复用两次，已有对应负向测试（`org-routes.test.ts` 的"blocks a company manager from pulling a lead in from a different company's group"）。
  2. **调用 `transferUserPosition` 时没有透传 actor 的真实 `role`/`departmentId`**，而是统一传一个"视同 ADMIN、无部门限定"的占位对象（`role: "ADMIN"`，仅用于这一次函数调用，不会持久化，审计记录里存的是 `actorId` 不是这个占位 `role`）。原因：`transferUserPosition` 内部专门为老 `Role.COMPANY_MANAGER` 写的部门级二次校验（`actor.departmentId` 比对）理解不了新公司管理员"跨多个部门"这种范围概念——新公司管理员的范围字段是 `companyId`，其 `departmentId` 天然是 `null`，如果照实传真实字段，这条老校验会把 `!actor.departmentId` 判成true直接拒绝，company/HQ两档新账号会永远调不动任何人。既然权限已经在上面两次 `canAppointOrTransferLead` 里判断过了，这里统一"绕过"内部旧校验，让新网关做唯一的权威判断，避免两套判断逻辑（新旧概念不兼容）打架。

- ~~**本次范围内明确没做、留给以后的缺口**：计划的5a任务清单本身没有列"新建 `Duty.COMPANY_MANAGER`/`Duty.HQ_MANAGER` 账号"的路由……~~ **已在阶段5a补充中做完**，见下方"阶段5a补充：管理员账号创建入口"小节。

- **测试**：新增3个测试文件。[org-permissions.test.ts](tests/unit/org-permissions.test.ts)（36个用例，每个 `can*` 函数正反用例都有，包括"部门管理员不能建公司"这类越级负向用例）、[org-routes.test.ts](tests/unit/org-routes.test.ts)（21个端到端路由用例，覆盖四档角色×五个新端点，含上面提到的"跨公司挖人"负向用例）、[department-manager-duty-backfill.test.ts](tests/unit/department-manager-duty-backfill.test.ts)（5个用例：只处理 duty 为空的账号、不覆盖已设置的、批量回填、二次执行幂等、空计划时不调用 `updateMany`）。
- 本次改动顺带暴露并修复了2处既有防护测试需要同步更新（不是本次引入的bug，是本次新增内容触发了既有守门测试）：[postgres-migration-immutability.test.ts](tests/unit/postgres-migration-immutability.test.ts) 硬编码的迁移数量 28→29；[security-authorization-coverage.test.ts](tests/unit/security-authorization-coverage.test.ts) 抓到 `org/structure/route.ts` 一处不可达的兜底分支用了裸 `NextResponse.json(...,{status:403})`，改成 `authorizationDenied()`（这条防护测试本身在阶段2之前就已存在，这次是第一次被新代码触发）。
- 验证：`npx tsc --noEmit` 干净；`npx vitest run` 全绿（169个测试文件、926 passed + 4 skipped，阶段4基线166个文件/864 passed，本次净增3个文件/62个用例，无回归）；`npm run build` 成功（新的5条 `org/*` 路由出现在构建产物路由清单里）；手工核对 `prisma/dev.db`：`User.companyId` 列+索引存在、外键约束指向 `Company`、`prisma migrate status` 报告"up to date"。

**阶段5a补充：管理员账号创建入口**（补上阶段5a完成时明确记录的缺口——新组织架构的三档管理员账号本身还没有创建路径，5b前端接不上真实账号；业务方确认的账号创建链条：总公司建公司、建公司管理员账号；公司管理员在本公司内建部门管理员账号，总公司可越级直接做；ADMIN 是总公司管理员账号唯一的自举入口，纯后端，不含UI）：

- **权限网关新增3个函数**：[org-permissions.ts](src/lib/org-permissions.ts) 的 `canCreateDepartmentManagerAccount(user, department)`（公司管理员限本公司名下部门、总公司管理员不限即可越级、ADMIN 兜底）、`canCreateCompanyManagerAccount(user)`（仅总公司管理员或 ADMIN——不是越级场景，是"公司管理员这一档本来就没有创建同档账号的权限"）、`canCreateHqManagerAccount(user)`（仅 ADMIN，纯系统自举，连现任总公司管理员本人也不行）。这三个函数需要读 `user.role === "ADMIN"`，是本文件"只读 Duty 不读 Role"既有约定之外**第二处**明确例外（第一处是 5.1 的 `canOperateCustomer`），文件头注释和函数各自的 JSDoc 都记了这一点，避免被当成遗漏。
- **新路由**（同样在 `src/app/api/org/` 下）：
  - `POST /api/org/department-managers`：给一个已存在的部门开 `Duty.DEPARTMENT_MANAGER` 账号，只建账号不建部门（部门要先经 `org/departments` 建好）。跟老的 `src/app/api/company/department-managers/route.ts`（老 `Role.COMPANY_MANAGER` 视角、按国家找小组核实市场）路径接近但完全不共享逻辑，两条路由并存。
  - `POST /api/org/company-managers`：给一个已存在的公司开 `Duty.COMPANY_MANAGER` 账号，仅总公司管理员/ADMIN。
  - `POST /api/org/hq-managers`：开 `Duty.HQ_MANAGER` 账号，仅 ADMIN，无范围参数。
  - 三条路由的账号创建 SHAPE（哈希密码、`mustChangePassword: true`、事务+`recordAudit`、`P2002` 冲突返回409）照抄阶段5a既有的 `org/companies`/`org/departments` 路由和老的 `admin/users/route.ts`/`company/department-managers/route.ts` 的约定，`role` 字段固定填 `"COMPANY_MANAGER"` 占位（跟阶段5a给老 `Role.COMPANY_MANAGER` 账号回填 `Duty.DEPARTMENT_MANAGER` 是同一个占位惯例——新权限网关不读这个字段，只是 schema 要求非空，不代表账号真实语义；计划文档阶段5小节原文这条决策本来只提到公司/总公司管理员两档，这次新增的部门管理员账号沿用同一个占位值保持一致，不另开分支）。
- **新增登录闸门**：[org/_auth.ts](src/app/api/org/_auth.ts) 的 `requireAdminOrOrgManagerRequest()`，跟既有 `requireOrgManagerRequest()` 是同一种"粗闸门 + `can*` 细判"结构，唯一区别是额外放行 `Role.ADMIN`（没有 duty，会被老闸门直接拒之门外）。没有为三条路由各自定制闸门——三者允许的调用方形状虽然不同（部门管理员账号：公司/总公司/ADMIN；公司管理员账号：总公司/ADMIN；总公司管理员账号：仅ADMIN），但都可以先用这同一个"登录了、且是ADMIN或某档组织管理者"的粗判断兜底，具体这一档能不能做，交给各自的 `can*` 函数（比如 `hq-managers` 路由下，一个真实的 `HQ_MANAGER` duty 账号会先通过这道粗闸门，再被 `canCreateHqManagerAccount` 拒绝，跟其它 `org/*` 路由的两层结构完全一致）。
- **测试**：[org-permissions.test.ts](tests/unit/org-permissions.test.ts) 新增17个用例（3个函数各自的正反用例，包括"公司管理员不能建同档公司管理员账号""部门管理员不能创建任何管理账号""ADMIN能创建HQ账号、现任HQ管理员本人不能"）；[org-routes.test.ts](tests/unit/org-routes.test.ts) 新增18个端到端路由用例（覆盖三条路由 × 各档调用方，含密码哈希校验——`verifyPassword` 反查、`mustChangePassword`/作用域字段核对、重复账号409、密码过短400、部门/公司不存在400）。
- 验证：`npx tsc --noEmit` 干净；`npx vitest run` 全绿（168个测试文件、959 passed + 4 skipped——基线是阶段4末删除死代码后的168个文件/924 passed，本次净增35个用例、无回归；测试文件数不变是因为新增用例加进了已有的 `org-permissions.test.ts`/`org-routes.test.ts`，没有新建测试文件）；`npm run build` 成功（新的3条 `org/*` 路由出现在构建产物路由清单里）。
- **阶段5a（含本次补充）实现完毕并已独立验证；阶段5b（组织管理UI）已经启动。**

**新版前端实测待办（2026-08-28，组员管理/岗位配对；阶段5b/6接入时处理）**：

1. **已解决 · 配对历史版本（2026-08-29）**：保留 `GroupOperatorReception` 作为当前配对指针，新增 `GroupOperatorReceptionHistory` 记录 `effectiveFrom/effectiveTo/createdById/reason`；迁移会把现有配对回填为首条历史。换配对和转岗清除配对时都会先关闭历史行，未变化的配对重复保存不会制造重复版本。这样旧查询不需要一次性大改，同时可以准确追溯某段时间谁与谁配合。
2. **已解决 · 换配对与客户交接分开（2026-08-29）**：修改当前配对不会批量改已明确的 `LeadCustomer.groupOperatorOwnerId`。新增 `/api/lead/collaborations/handoff`，必须先预览在办客户数量，再带同一数量明确确认；数量在确认前变化会返回冲突并要求重看。交接只覆盖未退群、未推专家、有效且仍在炒群阶段的客户，并写审计记录，新旧负责人和数量均可追查。
3. **P1 · 开账号/设岗位/配对还未接成一个真实事务**：v2-admin 原型已把“岗位+配对炒群”放进同一弹窗，视觉方向已确认；接真实后端时必须保证账号（或岗位）与配对一次保存，任一步失败都整体失败，避免出现岗位已生效但配对没保存的半成品。
4. **P1 · “待配对”缺少运行期提醒**：原型允许选择“未配对”，但只有下拉值，没有持续警告。应在组员卡片、组长待办和炒群工作台显示“待配对”；待配对时不做自动炒群归属，只允许组长之后补配或明确交接。
5. **P1 · 兼任人员的自配对含义要明确**：接粉+炒群兼任时允许本人承接自己的客户，界面显示“兼任·本人承接”；不能把本人当成普通候选人导致重复关系或看不懂。
6. **P2 · 操作按钮过度依赖图标**：新版成员卡片里的铅笔、钥匙图标仅靠外形辨认。改为“设置岗位”“重置密码”等文字按钮，或至少提供始终可读的标签和键盘焦点提示。
7. **P2 · 新版总览隐藏了账号运行信息**：按岗位分栏比旧表清楚，但用户名、启用/停用、最近登录不能彻底消失；保留在成员详情/账号管理抽屉里，日常总览不必铺满这些字段。
8. **验证要求**：新增测试锁定“一接粉只能有一个当前炒群”“一炒群可带多人”“换配对不改历史报表”“已在办客户不自动转人”“账号/岗位与配对事务回滚”“兼任本人承接”“待配对不自动归属”。

**替换现有服务器前的阻断清单（2026-08-28复核）**：

1. **P0 · 旧数据迁移决策与演练缺失**：需求文档最初写“旧数据不迁移”，但业务目标已变成未来替换现有服务器。必须先确认客户号码、人员账号、转组转岗历史、渠道、开单与全部资金流水的迁移范围；迁移脚本要能重复演练、对账并支持回滚，未完成前不能切换生产。
2. **P0 · 线上最新修复尚未做差异回放**：以现有服务器版本为基准，逐项核对新项目是否包含其后上线的修复，重点检查德国/美国/新加坡当地日期、跨日写入、转岗生效日、新建人员日期和跨组报表边界。不能只凭两个仓库的文件名判断，必须有时区边界测试。
3. **P1 · “不做考勤”尚未落实到代码清理**：需求文档7.2已确认考勤不做，但新项目仍保留考勤菜单、页面、接口、导出与模型。接前端时先隐藏所有入口；正式替换前再确认是保留数据库兼容层还是删除代码，不能让用户看到一个明确不再使用的功能。
4. **P1 · 两个v2项目只是原型**：`data-statistics-v2`/`data-statistics-v2-admin` 仍依赖 `mock-data.ts`，只能作为交互和视觉参考，不能作为已接通真实权限、真实数据库或可上线功能的证据。阶段5b/6必须逐页接真实API并用各档真实账号走通。
5. **P1 · v2仍有旧成本概念残留**：`data-statistics-v2` 仍出现 `$35`、数据成本、批次成本单价等旧文案/字段；接入真实后端时必须删除，不能把已从新后端移除的渠道成本逻辑重新带回来。
6. **已解决 · Git远程备份**：仓库现已配置 `origin = git@github.com:qq122728/data-statistics-private-v2.git`，并已推送 `main`；这项不再是阻断。后续仍要求每个完成阶段提交并推送后再审查。
7. **已解决 · 历史客户由谁录入**：已经改为各岗位按负责阶段自助认领、组长审核后生效；不再采用“全部由组长亲自补录”，阶段6按需求文档7.3实现。
8. **已完成本地实库演练 · SQLite→PostgreSQL 数据搬运链路（2026-08-29）**：`db:postgres:test:prepare` 会先把固定本地目标测试库清空，再从零重放正式 migrations；独立参考库由当前权威 PostgreSQL datamodel 生成，并补上 Prisma schema 无法表达的“一个小组只能有一名在职组长”部分唯一索引，随后比较目标库与参考库，能发现“schema 已改但忘写 migration”，不再拿两套同源迁移库做无意义比较。权威根 SQLite schema 生成独立搬运客户端，不再引用过期 `prisma/sqlite/schema.prisma`。复制计划覆盖除 `Session` 外的全部模型（包含 `Company`、`UserPosition`、`UserGroupMembership`、`ResourceChannelAccess`、配对历史），`Session` 明确清空且不复制，搬完需重新登录。目标清理、全表写入、父事件恢复和逐表全字段对账在同一个 PostgreSQL 事务内，失败整体回滚；目标库和参考库 URL 都严格限定到本机固定端口、固定库名、账号和 public schema。CI 新增 PostgreSQL 17 双库真实复制与结构比对。本机 PostgreSQL 17 双库实跑首次发现历史迁移与权威 schema 有 4 项索引差异：删除已由复合索引取代的 `LeadCustomer_isHistoricalRecord_idx`，并把 3 个因 PostgreSQL 63 字节截断而与 Prisma 目标命名不一致的索引改名；新增 `20260829080000_align_postgres_indexes_with_schema` 修正迁移，不改写任何已登记历史迁移。修正后 31 条正式迁移可从空库完整重放，结构对比为 `No difference detected`；当前本地 SQLite 的 193 个客户、32 个订单、461 条客户进度及其余全部业务表均已复制并逐表逐字段对账通过，`Session=0`；当前版本新增字段和关联夹具、组长唯一约束、迁移 checksum 与安全门禁测试均通过。大白话：搬家车已经真的跑完一趟，货物数量和每箱内容都对上了；这次还提前修好了三个“箱子标签”和一个过期标签的问题，仍未接触服务器数据库。
   - **2026-08-30 最新复演**：在当前 42 条 PostgreSQL 正式迁移和最新本地数据上重新从空库演练。首次执行发现 `20260830143000_backfill_expert_workflow_stage` 把文本 `CASE` 直接写入 PostgreSQL enum，已补显式 `::"ExpertWorkflowStage"` 并更新登记 checksum；同时修正报表演示脚本在已有 `demo_lead` 的美国一组重复创建第二名在职组长，以及 `db:postgres:test:check-drift` 漏传固定本地双库 URL。修正后 42 条迁移完整重放、结构无漂移；32 张业务表逐表逐字段一致，样本为 66 账号、201 客户、33 订单、515 条每日数据、523 个数据版本，账号凭证摘要全部一致，9 类原账号真实登录 PostgreSQL 演练后端成功，完整单测为 186 文件/1077 测试通过（4 skipped）。随后只读连接生产服务器，把当前 PostgreSQL 在线一致性快照直接传到本机隔离目录；线上仍保持运行且未执行迁移、未改数据。真实快照从 27 条迁移升级到 42 条全部成功，52 账号、309 客户、32 订单、794 条旧事件及账号密码/客户归属摘要迁移前后相同，39 条充值/出金完整进入独立资金表；新版后端在该副本上的七类角色代表接口均为 200。正式切换与回滚步骤见 `docs/operations/server-replacement-cutover.md`。
9. **核心已解决、跨午夜刷新待补 · 目标小组当地日期（2026-08-29）**：新增 `resolveGroupBusinessTimezone/resolveGroupBusinessDate`；三个建号入口、管理员转岗和组织组长任命均按目标小组当地今天写入/校验。`team-performance` 与 `group-daily-detail` 的今日、近7天、当月也按每个小组独立生成周期，再统一查询、逐组过滤；柏林、新加坡、纽约同一 UTC 瞬间和纽约夏令时已有回归测试。尚未覆盖页面长时间停留跨过当地午夜后的自动刷新，以及审计列出的其他目标对象页面，后续作为 P1 独立处理。

**阶段5b真实组织管理UI第一切片（2026-08-29）**：`/company-organization` 已从旧 `/api/company/*` 和原型数据切换到真实 `/api/org/*`。总公司/公司/部门管理员按 `Duty` 看到各自范围；支持按权限建公司、部门、小组、公司管理员和部门管理员账号；未挂 `companyId` 的旧部门独立展示。账号身份和公司/部门名称也改为按 `Duty`/新范围字段显示。组长任免暂未接入：当前结构接口没有返回安全候选人员，下一切片先补范围受控的成员读取接口，再做真实选择器，禁止让用户手填 `userId`。

**阶段5b组织与配对管理第二切片（2026-08-29）**：新增范围受控的 `GET /api/org/lead-candidates?groupId=`，候选人的现小组与目标小组都必须通过同一组织权限网关，只返回姓名、岗位和现小组等选择器字段；`/company-organization` 已用真实下拉选择器接通既有组长任命/调组路由，不再手填 `userId`。组长 `/team-members` 的配对区改为按接粉员显示，支持明确保存“待配对”、新炒群账号建好后批量勾选待配对接粉、兼任人员显示“兼任·本人承接”；待配对有持续醒目警告，且不会自动指定炒群。配对变更仍不转移已有客户；在办客户交接单独调用既有 `/api/lead/collaborations/handoff`，页面必须先预览数量，随后把同一 `expectedCount` 带回确认，数量变化时由后端拒绝并要求重看。

**阶段5b新版 v2-admin 真实接入第一切片（2026-08-29）**：`data-statistics-v2-admin` 保留新版视觉，通过同源 `/api/*` 代理连接本重写后端；新增 `GET /api/auth/me` 只返回当前账号、岗位/职务和组织范围，不暴露密码散列。真实账号登录后不再允许原型的“查看身份”随意切换，导航只显示已接真实接口的页面：组长可管理真实组员、岗位与配对并单独交接在办客户；各档组织管理员读取真实组织树、候选人并执行组长任免/调组。`POST/PATCH /api/lead/members` 新增 `pairedGroupOperatorId`，账号/岗位与配对在同一个数据库事务内保存；普通接粉允许明确“待配对”，接粉+炒群兼任且未另选人员时自动保存为“兼任·本人承接”，无效或跨组炒群会在创建账号前被拒绝，避免账号已经落库、配对失败的半成品。原型中尚未接真实接口的报表/通知/设备页面仍保留作视觉参考，但真实账号导航已隐藏。

**阶段5b后端补充：给空缺小组开设全新组长账号（2026-08-29）**：新增 `POST /api/org/group-leads`，解决既有 `groups/[groupId]/lead` 只能任命/调动现有人、空组没有候选人时无法开号的缺口。请求体为 `{ groupId, username, name, password, effectiveOn }`；部门管理员限本部门，公司管理员限本公司，总公司管理员不限，ADMIN 仅作系统初始化兜底。目标小组已有启用组长时返回409，不能覆盖，必须走任免/调动流程。创建 `User`、`Duty.LEAD`、目标小组绑定、`UserRoleAssignment`、`UserGroupMembership`、入职/生效日期和审计记录全部放在同一个 Serializable 事务里；临时密码只存哈希并强制首次登录修改，生效日期按目标小组当地今天限制。新版 v2-admin 的UI按钮仍由当前UI任务接入；接入时直接调用本接口，不要再沿用原型里只改内存状态的 `createGroupLeadAccount`。

**阶段5b真实组织统计接入第一切片（2026-08-29）**：新增只读 `GET /api/org/reporting`，组长/部门管理员/公司管理员/总公司管理员分别只取得本组、本部门、本公司或全部小组；资源部管理员明确拒绝，不能借统计入口看到客户业绩。接口复用 `team-performance` 的权威事实流，同一次查询同时返回小组汇总和逐人明细，消除 v2-admin 原型“上层硬编码了汇总、点进小组却没有人员数据”的两套数据源问题。`today/7d/month` 等周期通过 `buildGroupBusinessPeriods` 按每个小组当地时区独立换算；空组保留为全 0 行。v2-admin 的部门、公司、总公司三个只读明细页已接入；组长页的“数据汇总”也已接入同一接口，并保留“渠道数据核对/客户进度”入口。后两项尚未接真实接口，页面会明确标为演示且不会写库，避免把部分真实页面误报成全部完成。

**阶段5b组长真实渠道统计（2026-08-29）**：新增只读 `GET /api/lead/channel-reporting`，仅在职组长可读取本组渠道，日期按本组当地时区换算；v2-admin 的“渠道数据核对”已替换原型数据，资源部审核提交尚未实现，因此明确显示只读且不再保留假的“发送审核成功”操作。接入实测同时发现并修复 `channel-analysis.ts` 的有效粉双扣：电话账本在 canonical events 中已经排除无效/低金额/无 WS，渠道汇总原来又把同一批改判无效数据减了一次；现在只从历史汇总的 `EFFECTIVE_FANS` 补扣后来改判量，电话账本有效数不重复扣。回归测试锁定“1 个有效 + 1 个无效 = 添加 2、有效 1、无效 1”。

**阶段5b组长真实客户进度读取（2026-08-29）**：新增只读 `GET /api/lead/customer-reporting`，只允许在职组长读取自己小组，按接粉、炒群、专家三个阶段返回真实客户档案、三岗归属、来源、关键日期、最近三条活动和订单金额；支持号码/姓名搜索及每页 50 条分页。v2-admin 的“客户进度”已替换原型名单，页面明确标为只读，资源部管理员没有入口且后端也会拒绝。当前只完成真实读取，回复、入群、推专家、专家状态及资金修改仍须分批接入现有后端动作，不能把这次切片理解成完整工作台已经完成。

**阶段5b组长客户跟进备注（2026-08-29）**：v2-admin 的真实客户进度已接入三种低风险写入：接粉阶段写客户备注、炒群阶段写当日群内进度、专家阶段写下一步计划和计划日期。前端没有新造一套业务逻辑，而是复用 `/api/leads/[leadId]` 现有 workflow：事务内重新读取在职身份、只允许组长修改本组客户，并按小组时区写业务日期；炒群进度同一天重复保存会更新当天记录，专家计划会留下 `PLAN_UPDATED` 活动。实测还补上了日期输入的 `input` 事件，避免某些浏览器出现“日期框看着有值但提交为空”。回复、入群、推专家、阶段跳转和资金动作仍未接入新版页面。

**阶段5b组长确认回复/入群（2026-08-29）**：接粉进度现在同时显示待回复和已回复待入群客户，已归档客户排除；组长可经过二次确认把待回复标为已回复，再确认入群，复用现有 `reply`/`joinGroup` workflow，因此仍要求客户已有接粉设备、仍按小组当地日期计数，并在事务内复查组长在职状态与本组范围。客户入群后自动移出接粉列表并进入炒群列表。真实浏览器串行走通两步后还发现“当前状态已入群、最近情况却回退显示旧等待回复备注”，已把 `REPLIED`/`JOINED_GROUP` 纳入最近活动，并为无文字的活动显示“已确认回复/已确认入群”。

**阶段5b组长退群/推专家（2026-08-29）**：炒群进度对仍在群客户开放二次确认退群和推专家；退群可填备注，推专家可选择本组在职专家，不选择则由组长本人承接。前端复用已有 `leaveGroup`/`introduceExpert` workflow，后端继续负责入群日期顺序、本组专家范围、历史计数和事务权限校验；已推专家或已退群后对应按钮自动消失。客户进度最近活动补入 `LEFT_GROUP`/`EXPERT_INTRODUCED`，避免动作成功后仍展示旧跟进文字。真实浏览器已分别用两名本地演示客户走通退群和指定专家。

**阶段5b组长专家阶段流转（2026-08-29）**：专家进度已按权威顺序接入“开始接待（必填本次专家设备号）→开始追踪→转待注册→确认注册”；每一步只在当前状态允许时显示，危险跳步由后端 workflow 再次拒绝，组长仍只能处理本组客户。页面没有复制新的日期算法，设备号、专家联系日和注册日继续由既有事务按小组当地业务日期落库；注册后进入“已注册·待开单”，不会在本切片自动开单或写金额。真实浏览器已用本地演示客户完整走通四步，后端岗位边界 33 项测试及 v2-admin 类型检查/生产构建通过。开单、首充、续充、出金仍属于下一独立切片。

**阶段5b组长开单与资金登记（2026-08-29）**：专家进度新增“登记开单”“登记续充”“登记出金”，复用既有 `/api/customer-orders` 和 `/api/customer-finance` 权限、事务、日期和防重规则。金额输入按十进制字符串精确转成美分，不使用浮点乘法；开单只写一次订单首充，续充自动取下一续充序号，出金不改变首充。同步修复组长客户进度金额口径：订单生成的首充 `RECHARGE` 事件不再混入续充合计，只有 `continuationNumber` 非空的事件才算续充。真实浏览器用 `1148` 开单后显示 `$1,148.00`，再续充 `$100.00` 后显示 `$1,248.00`；实库核对为首充 `114800` 美分、续充 `10000` 美分，未重复计算。开单纠错/逐笔资金作废仍复用旧后端能力，尚未接到新版组长页面。

**阶段5b组长倒序纠错（2026-08-29）**：新版客户进度已接真实纠错入口，按当前最后一步只开放安全撤销：撤销回复、入群、退群、推专家、专家接待、注册，以及逐笔作废续充/出金和最后作废开单。所有操作必须填写原因；有续充或出金时页面只显示逐笔资金作废，清完后才出现“作废开单和首充”，避免跨步骤连删。客户报表只返回可纠错的有效资金事件 ID，不把首充事件混入逐笔列表。同步修复专家纠错的状态残留：撤销接待会一并回到排队中并清设备号/追踪时间，撤销推专家会清空专家工作流阶段。真实浏览器已验证作废本地 `$100` 续充后总金额从 `$1,248.00` 回到 `$1,148.00`，下一步自动只显示作废开单；41 项相关后端测试与 v2-admin 构建通过。

**阶段6一线真实工作台第一切片（2026-08-29）**：接粉、炒群、专家三个工作台已不再读取原型客户。接粉本人可确认回复、确认入群和归档无效客户；没有设备号时不能确认回复，没有当前接粉/炒群配对时不能入群。炒群本人只看到按冻结归属与当前配对归到自己的客户，可填写每日进度、退群、选择本组专家并推送。专家本人只看到明确分配给自己的客户，可按顺序推进专家阶段、维护计划、开单、续充和出金。三个角色均由后端再次校验本人身份和小组边界；本地页面已分别用演示炒群、演示专家账号登录验证，关键接口 42 项测试通过。

**阶段6历史客户认领/审核第一切片（2026-08-29）**：新增各岗位按自己负责阶段提交历史客户认领、组长只审核本组记录的真实接口；号码全局去重，待审核期间锁定且不进工作待办、不算业绩，审核通过后按申报阶段进入后续跟进，但以前已发生的推专家/注册等不补算个人业绩。新版组长“审核中心”已接真实待审核列表、通过和退回，退回必须填写原因。本地组长账号已验证真实空列表。后续v2前端接入见下一条；仍待补退回后的编辑重提，以及“历史已开单”专用认领分支。

**阶段6历史客户认领v2前端接入（2026-08-29）**：接粉、炒群、专家三个真实工作台都恢复v2原型“点击按钮后在当前页面内展开”的认领方式，共用一个真实组件但由后端限制岗位阶段：接粉仅未回复/已回复，炒群仅已进群，专家仅已推专家/已注册。表单只允许选择现有真实渠道，不允许一线新建历史汇总；没有金额字段；提交后进入组长审核，通过前保持锁定且不进待办/业绩。人员候选按所选历史状态日期读取 `UserPosition`/`UserGroupMembership` 历史，转岗后仍能选择当时属于该组的负责人，后端提交时再次按同一日期和岗位复核，不能只靠前端下拉框。本人最近认领状态会显示在面板底部。三种真实演示账号已逐个登录并验证对应阶段和负责人字段，历史认领4项后端测试、前端类型检查与生产构建通过。仍待补：退回后的编辑重提、“历史已开单”专用分支，以及其余v2客户抽屉/显示密度等视觉细节。

**管理员客户进度独立入口与统计审核文案收口（2026-08-30，本地）**：部门、公司、总公司管理员左侧新增独立“客户进度”入口，按各自后端权限范围逐级选择具体小组后只读查看；“组内明细/部门明细/公司明细”只保留数据汇总与渠道核对，不再内嵌重复的客户跟进标签。资源部仍无客户进度入口。需求文档同时统一为当前流程：接粉每日数据由资源部核对确认，炒群/专家保存即生效，不再使用“组长审核/已审核数据”描述；客户通讯录与每日统计继续完全独立。

**2026-08-30业务口径纠正（覆盖上段审核描述）**：客户号码/客户进度只是员工自己的通讯录，保存后直接生效，组长和部门/公司/总公司管理账号只读查看；资源部与财务完全看不到。客户新增、历史补录、阶段推进、资金明细及客户行编辑均不发送审核，也永远不写每日统计。`POST/PATCH /api/historical-claims` 已按此改为直接建立/更新有效客户档案，旧的历史认领审核接口只为兼容旧记录保留，不再承接新提交，管理端审核中心也已移除该入口。

**2026-08-30历史已开单口径再次确认（覆盖阶段6“历史已开单专用分支”待办）**：不建设“历史已开单基线”或历史占位订单。旧客户以前的开单数、首充和续充金额已经在历史汇总统计中，以汇总数字正确为准；员工只需在客户进度工作台自行补入具体客户并选择真实当前状态。补成“已开单”仅表示通讯录状态，不创建 `CustomerOrder`，不补具体号码的历史资金，也不写每日统计。生产库已经执行过 `20260827193000_add_historical_order_baseline`，迁移文件仍需作为既有账本证据保留，但新版业务代码不得使用该字段重新开放历史已开单功能；数据库兼容字段待新版稳定且不再需要旧版本回滚后再评估清理。

**独立每日统计直接核对（2026-08-30最终确认，覆盖此前两级确认）**：员工每日统计只需保存，不再提交组长；组长没有逐条审核和整组发送入口。接粉保存后直接进入对应渠道资源部的 `RESOURCE_PENDING` 列表，资源部只保留“确认无误”，确认后才切换 `approvedRevisionId`；发现错误时线下联系员工，员工可在待核对状态直接保存新版，资源部刷新读取最新版本。炒群和专家保存后立即成为 `APPROVED` 正式版本。已存在的旧流程状态通过 `20260830180000_simplify_daily_stat_review` 数据迁移转换，不删除任何历史版本。真实接口只保留员工 `GET/POST /api/daily-stats` 与资源部 `GET/PATCH /api/resource/daily-stats`；旧的 `/api/lead/daily-stats` 和管理端每日审核中心已移除。

**部署前新增阻断（2026-08-29晚间确认）**：客户进度工作台的三个岗位区都要提供“添加一行”和可审计编辑；接粉、炒群、专家分别只能选择本岗位合法状态，且任何新增/编辑都不得联动每日统计。所有客户编号入口还要统一为“去除非数字后只取末6位”，不能继续出现批量导入已经取6位、单个/历史录入仍保存完整号码的混合口径。线上只读预检：现有304个客户按末6位转换后没有碰撞；本地演示库193个客户中有181个落入碰撞组，因此不能只改格式函数，必须配套碰撞门禁、一次性数据转换和演示数据重建。当前线上只有单个 `data-statistics` 服务监听3000，而新版仍是后端3003、一线3000、管理端3002三个独立项目；生产服务、Nginx路由、回滚与备份迁移演练尚未建立。以上未完成前禁止替换线上版本。

**此前7项技术问题的记录位置**：每日明细/主榜单三层归属见阶段4第五轮记录；历史客户待注册/待开单见需求文档7.3及 `task_da86994c`；异常退群率归属轴线见阶段4第三轮记录；误录作废护栏见本文“客户主记录”下方待办；旧渠道价格文案见阶段3记录；PostgreSQL演练链路见阶段3 Stage B记录；老板日报/导出逐日重复查询见阶段4第三轮性能待办。

**生产替换兼容演练（2026-08-30，仅本地，未修改服务器）**：补齐线上已经存在但重写仓库缺失的两条迁移账本，并在新版移除旧渠道计价字段前后增加“先归档、再恢复兼容列”的迁移，保证切换失败时旧版本仍能读取原数据库。新增旧账号结构回填迁移：只补空缺的岗位、主角色和岗位记录，不改用户名与密码哈希。将线上 PostgreSQL 通过只读 `pg_dump` 恢复到本地隔离库后，完整执行 27→39 条迁移；迁移前后的用户51、客户304、事件788行数及用户/客户/事件/渠道计价/批次计价五组校验哈希完全一致，回填得到42条岗位记录、11个岗位标识、0个缺失主角色。再用旧项目的 Prisma 客户端读取升级后的本地副本，用户、客户、渠道、批次和订单均可正常查询，证明数据库回滚兼容可用。SQLite→PostgreSQL 搬运脚本同时修复了每日统计循环指针更新导致 `updatedAt` 被意外改变的问题，并增加只报告记录ID和差异字段名的安全诊断。以上只是数据库兼容地基；正式切换仍必须先完成新鲜备份验证、三套前端/后端生产路由和最终切换回滚清单。

**新版工作台真实入口收口（2026-08-30，仅本地）**：一线端 `data-statistics-v2/app/page.tsx` 已移除不可达的整套演示客户、历史批次和假资金状态，只保留号码导入、接粉跟进、炒群、专家、每日统计、通知、设备、个人业绩和榜单的真实接口组件；导入表格仅需的类型和格式函数移到 `lib/customer-import.ts`，不再从 `mock-data.ts` 带入演示记录。管理端 `data-statistics-v2-admin/app/page.tsx` 同样重建为真实接口入口，删除运行时假公司、假部门、假客户、假审核和假资金状态；组长、部门/公司/总公司管理员、资源部按真实身份进入各自导航。登录接口新增 `workspace` 分流结果，三端登录页会自动进入一线端或管理端；`SESSION_COOKIE_DOMAIN` 仅允许配置纯父域名，用于可信同主域子域之间共享 HttpOnly 会话，本地联调使用 `.localtest.me`。本地接口验证：接粉返回 `FRONTLINE`、组长返回 `ADMIN`，Cookie 带 `Domain=.localtest.me` 且不输出会话值。正式域名未确定前不得把示例域名直接部署。

**系统管理员身份收口（2026-08-30，业务方确认）**：业务上不再区分“系统管理员”和“总公司管理员”。旧 `Role.ADMIN` 登录新版管理端时直接映射为总公司管理员工作台，组织结构、组织统计、精英榜、新建公司/部门/小组及组长人事均按全局范围处理；前端不提供另一套独立的系统后台。数据库里的 `Role.ADMIN` 与 `Duty.HQ_MANAGER` 暂时保留兼容旧账号和已有接口，属于技术标记差异，不代表两个业务层级。

**管理端 3010 原型外壳恢复（2026-08-30，仅本地）**：`data-statistics-v2-admin` 已把此前过度统一的组织汇总页面恢复为各身份在 3010 原型中的专用外壳。组长数据汇总、渠道核对继续使用“指标为行、总计和员工为列”的矩阵；部门、公司、总公司及资源部使用各自层级为行的完整指标表；通知中心与组内设备重新使用原型式列表、分页签和弹窗。数据仍来自真实后端，不恢复 `mock-data.ts`，每日数据填写保留为新增独立页面，管理端客户进度继续保持单独只读入口。`GET /api/org/reporting` 新增逐日 `days` 和成员完整指标，`GET /api/lead/channel-reporting` 新增渠道内成员拆分；新增 `/api/group-devices` 供组长管理实体设备和接粉分配。生产构建、TypeScript 与全量测试均通过（184 个测试文件、1062 项测试），未部署服务器。

**汇总与渠道逐日明细恢复（2026-08-30，仅本地）**：数据汇总在组长及管理员下钻到具体小组后统一显示“区间汇总 + 每日明细”；组长渠道接口和组织管理员渠道接口新增 `days`，渠道核对页面也恢复逐日矩阵；资源部“小组明细”同步显示授权渠道的区间汇总和每日明细。演示数据改为 4 个小组连续 21 天、每天 6 条岗位记录（每组 126 条），并修正炒群演示行误写接粉 `joinCount` 导致进群重复累计的问题。汇总页、组织下钻和资源部的拉群率分母统一为回复数，符合需求文档 6.2。

**上线前全流程复查第一轮修复（2026-08-30，仅本地）**：客户通讯录与每日统计已物理解耦，新增 `CustomerFinanceEvent` 独立保存续充/出金事实；SQLite/PostgreSQL 迁移从旧 `MetricEvent` 安全回填但不删除旧账，新版客户导入、推进、开单、资金和删除不再新增统计事件。接粉确认入群交棒后由后端强制只读；号码统一清洗后取末 6 位；旧历史开单/资金入口关闭。人员同组换岗和跨组调动统一经过 `transferUserPosition`，按组长/部门/公司/HQ范围授权，预览三段在办客户、选择原组合法接手人并保留岗位和小组历史。资源部待核对列表与确认动作统一按渠道类型授权；“当前在群”在组织、渠道和资源汇总中统一按每条业务线截至结束日的最近正式快照聚合；管理端日期使用后端小组当地日期。全量回归为 184 个测试文件、1073 项通过，双前端和后端生产构建、PostgreSQL 42 条迁移校验及迁移差异检查均通过。仍待下一轮处理：客户跟进同日覆盖更新缺少完整的 before/after/修改人/岗位/时间/原因修订链；正式上线前还需使用最新线上只读备份重跑迁移、对账、旧账号登录和回滚演练。

**现有域名三服务入口决策（2026-08-30，仅本地）**：业务方确认继续使用现有正式域名，不新增前台/管理端子域名。公网路径固定为一线端 `/`、管理端 `/admin`、后端 `/api`；服务器内部端口分别为 3000、3002、3003。管理端通过 Next.js 构建时 `APP_BASE_PATH=/admin` 生成正确的静态资源和客户端路由，双前端登录分流在非 `.localtest.me` 环境下改为同源根路径或同源 `/admin/`；`/api` rewrite 明确不受管理端 basePath 前缀影响。本地用单一域名反向代理模拟后，总公司管理员登录、共享 Cookie、`/admin` 页面、`/admin/_next` 静态资源和 `/api/auth/me` 均通过。正式服务器尚未修改；切换步骤与回滚要求记录在 `docs/operations/server-replacement-cutover.md`。

**现有域名三服务正式切换（2026-08-30，已上线）**：停写后制作最终 PostgreSQL 自定义格式备份并复制到本机，校验值一致；生产库 27→42 条迁移全部成功。迁移前后账号52、小组6、渠道31、批次48、客户310、订单32、旧指标事件796及有效订单首充合计1,825,700美分完全一致。systemd 现分别管理后端3003、一线端3000、管理端3002，旧单体服务已停止并禁用但发布目录保留；Nginx 已按 `/api`、`/admin`、`/` 分流。服务器隔离副本曾用七类现有账号代表会话验证管理员、部门管理员、资源部、组长、接粉、炒群、专家接口均为200；公网主页、登录、管理端和静态资源正常。上线观察发现旧浏览器标签仍访问 `/dashboard` 等单体路径并收到404，已补统一307到新版根工作台的兼容层，再由登录身份自动分流；切换后无5xx、三项服务无重启和warning日志。

**现网组织层级纠正与多部门管理员（2026-08-30）**：切换后“全局人事”空白并非数据丢失，而是旧库只有一个未挂公司的“恒升部”部门，新页面只渲染 Company 层。业务方确认“恒升部”实际是公司，下设美国部、德国部、新加坡部；美国3组、德国2组、新加坡1组按既有 `countryCode` 原位调整，组、员工、渠道、批次、客户、订单和业绩主键均不重建。欢喜调整为恒升部公司管理员；王麻子需要同时管理美国部和新加坡部且不可见德国部，因此新增 `UserManagedDepartment` 多部门授权关系，`User.departmentId` 仅保留为兼容主部门。组织树、组织汇总、渠道明细、精英榜、通知和人员调动统一读取授权部门集合；新建部门管理员账号自动写入首个授权部门。正式数据调整脚本为 `scripts/ops/migrate-hengsheng-organization.mjs`，必须显式确认且在事务前后核对核心数量。本地用正式切换备份副本演练通过：欢喜可见6组，王麻子只见美国+新加坡4组，访问德国渠道接口为403；账号52、小组6、渠道31、批次48、客户310、订单32、旧指标事件796全部不变。
- 2026-08-30：生产旧版 `MetricEvent` 独立统计账迁入新版 `DailyStatEntry` 时，只复制 `derivedFromLedger=false` 的人工统计事实；客户进度自动生成的兼容事件明确排除，防止客户进度重新污染每日统计。迁移脚本为 `scripts/ops/backfill-legacy-daily-stats.mjs`，带确认开关、幂等前缀和添加数据总数对账。
- 2026-08-30：新旧系统切换前已经导入的号码及当时已有客户阶段，通过 `scripts/ops/backfill-transition-customer-stats.mjs` 一次性结转到每日统计；脚本使用独立幂等前缀，之后的新号码导入仍不得自动改变统计。
- 2026-08-30：`saveDailyStat` 允许原员工更正 `legacy-metric-v1` / `transition-customer-v1` 迁入记录；首次保存会改成标准 identityKey，仍保留旧版本和接粉资源部复核规则。
- 2026-08-30：每日数据纠错按记录的历史岗位授权；员工当前已从接粉转为炒群时，仍可修改本人原接粉记录，但不能新建无权限岗位的数据或修改他人记录。
- 2026-08-30：新版总公司管理端补回“渠道设置”；`GET /api/admin/channels` 输出去重后的真实全局渠道目录，新增/改名/启停继续复用原高风险写接口。`ADMIN` 与 `duty=HQ_MANAGER` 使用相同全局权限和密码复核规则。
