# 代码地图

> 后端重写工程的共享上下文文档。每完成一个阶段，如果改动触及了这里描述的东西，回来同步更新。
> 配套文档：`需求文档.md`（权威规格）、`旧系统业务规则清单.md`（旧代码知识存档）、`/Users/aaaa/.claude/plans/merry-sauteeing-cook.md`（分阶段执行计划）。

---

## 项目结构

```
prisma/
  schema.prisma          ← 权威 schema（SQLite dev）
  postgres/schema.prisma ← 生产 schema，与根 schema 字段完全一致，只差 provider/generator
  sqlite/schema.prisma   ← 过时的分叉，重写完成后应删除，别照它改东西
  migrations/            ← 63 个迁移（根），命名能看出"纠错文化"（add_void_corrections、
                            freeze_group_operator_ownership 等）
src/
  app/
    (app)/                ← 登录后页面（team-performance、personal-data、reports…）
    api/                  ← 53 个路由文件，详见下面"API 路由"
  components/             ← 按域分：entry/、reports/、analytics/、admin/、shell/、history/、lead/…
  lib/
    auth.ts               ← 认证入口
    role-access.ts        ← 角色判断辅助函数（多角色叠加）
    permissions.ts        ← 记录级权限判断（"这条数据是不是我的"）
    report-scope.ts        ← 报表可见范围判断
    finance.ts             ← 【阶段3要删】成本/返点计算
    settings.ts             ← 系统设置（含仍在用的"报表模式"开关，别当死代码删）
    analytics/
      canonical-events.ts          ← 漏斗事实流
      role-rankings.ts             ← 个人+小组排行榜（含要删的成本字段）
      performance-leaderboard-query.ts ← 原生SQL小组榜（也含要删的成本计算，跟上面重复实现）
tests/unit/                ← 863 个测试，任何阶段结束都不能倒退
scripts/ops/               ← 只读运维脚本（对账、审计），仿照 audit-batch-snapshots.mjs 的模式
```

---

## 数据模型地图（当前状态，重写前）

**组织架构**：`Company`(阶段1新增) → `Department`(含 countryCode/timezone，=国家，新增可空 `companyId`) → `TeamGroup`(含转化率阈值配置)。旧 Department 行 `companyId` 为 null，UI 尚未接入，阶段5才会真正挂上公司层级。

**人员与角色**：`User.role`(单一 enum，含 ADMIN/RESOURCE_MANAGER/COMPANY_MANAGER/FINANCE/HR/LEAD/RECEPTION/GROUP_OPERATOR/EXPERT，岗位职务混在一起，**暂未废弃**) + `User.duty`(阶段1新增可空 `Duty` enum，管人的"帽子") + `UserRoleAssignment`(额外岗位) + `UserGroupMembership`(调组历史表，选填/不强制，**暂未废弃，暂未接线**) + `UserPosition`(阶段1新增，**强制的岗位历史表**，唯一约束 `[userId, effectiveFrom]`，目前还没有写入方，阶段2要落地 `transferUserPosition` 服务函数才会真正产生数据)。`Position` enum(RECEPTION/GROUP_OPERATOR/EXPERT) 与 `Duty` enum(LEAD/DEPARTMENT_MANAGER/COMPANY_MANAGER/HQ_MANAGER/RESOURCE_MANAGER/FINANCE) 阶段1新增，目前均未在任何服务层代码中被读取或写入（纯 schema）。

**客户主记录**：`LeadCustomer`——接粉/炒群/专家三段状态都在同一张表上（`groupStatus`、`expertWorkflowStage` 等），`ownerId/expertOwnerId/groupOperatorOwnerId/attributionOwnerId` 都是**活外键**（指向 User 当前状态，不冻结，阶段2要接冻结机制）。已有 `isHistoricalRecord` 系列字段支持历史补录分段，阶段1新增 `historicalReviewStatus/historicalReviewedById/historicalReviewedAt`(审核字段，模式抄自 `InvalidFanReport`)，目前也还没有写入方，阶段6要落地组长审核 API 才会用上。`receptionCategory`(PENDING/VALID/INVALID/LOW_AMOUNT/NO_WS) 支持无效库——**阶段1已验证并修复**：[mutations.ts](src/lib/customer-workflow/mutations.ts) 原来只要客户有回复/入群等下游进度就拒绝改判为 LOW_AMOUNT/NO_WS，与需求文档3.1.1的典型场景（跟进过程中才发现问题）冲突，已放开到"只在客户已进入专家环节或已开单时才拒绝"（因为那时候改判无效会连带锁死后续专家操作，是真实的功能冲突）；回复/入群阶段的客户改判无效不受影响，符合"仍可继续跟进"。`canonical-events.ts` 已正确把非VALID客户排除出统计口径，恢复路径(`restoreValid`/`classifyReception`→VALID)也已存在，均已验证。

**订单与资金**：`CustomerOrder`(开单记录，1对1 LeadCustomer) + `MetricEvent`(多态事件表，`kind` 区分漏斗计数/入金/出金/续充，`enteredById` 固定不变、`voidedAt` 软删模式)。

**批次与渠道**：`SourceBatch`(冻结快照的范例——建批次时复制 `Channel` 的价格/返点，之后改价不影响历史) + `Channel`(活的定价配置，阶段3要砍成本相关字段)。

**审核类**：`InvalidFanReport`(批次级废号审核，`PENDING/APPROVED/RETURNED` + 完整前后快照审计表 `InvalidFanReportAudit`)——这是历史客户补录审核（需求文档7.3）要抄的模板。`AuditLog`(通用日志)、`LeadActivity`(客户时间线，22种事件类型)、`LeadException`(导入异常)。

**设备**：`Device`(实体设备号) + `DeviceAccount`(聊天号码档案)——两者都是活外键，无历史表。

**专家8段流水线**：`ExpertWorkflowStage` enum = `QUEUED/MATERIALS/TRACKING/PENDING_REGISTRATION/PENDING_ORDER/DECLINED_DEPOSIT/ORDERED/STALLED`——**阶段1已验证**：[customer-workflow/mutations.ts:212-226](src/lib/customer-workflow/mutations.ts:212) 里 `markExpertStalled`/`markNoInitialDeposit` 的前置校验用 `registeredOn`/`customerOrder` 代理字段实现，效果上等价于"DECLINED_DEPOSIT 只能从 PENDING_ORDER 触发、STALLED 只能从 ORDERED 触发"，符合需求文档8.2语义，不需要改数据模型或服务层。唯一遗留：UI 文案仍是旧的"不愿充/杀不动"，没改成"未成交/停止维护"，留给阶段6前端接入时一并改。

---

## 认证与权限（现状，阶段5前不大改）

- `auth.ts`：`requireUser()`/`requireRole(...roles)`，session-cookie，7天过期。
- 角色判断三种写法并存：页面内联 `if(user.role!==X)`、`role-access.ts` 的角色集合+辅助函数（`hasAssignedRole`/`hasAnyRole`）、`auth.ts` 的 `requireRole`。
- 记录级权限：`permissions.ts` 提供 `canWriteCustomerRevenue`/`canReadReportGroup`/`findLivePermissionUser`(写事务内重查活权限，防止会话过期期间越权)；多数路由仍是手写 Prisma where 片段（如 `customer-orders/route.ts` 的 `customerScope(user)`）。
- 新增的组织架构/岗位转移/历史补录审核路由，从阶段1起就用统一权限网关写，不追加第四种写法。

---

## API 路由清单（53个文件，按域分组）

- **auth/**：login、logout、change-password
- **leads/**：CRUD + check + downstream-progress（客户主流程）
- **customer-orders/**、**customer-finance/**：开单与资金
- **batches/**、**channels/**：批次与渠道（含广告花费）
- **admin/**：departments、groups、users(+transfer)、channels、settings、risk-*、audit-logs、customer-follow-up
- **company/**：department-managers、groups、leads（公司管理员视角）
- **lead/**：collaborations、conversion-standards、expert-assignments、members、performance-details（组长视角）
- **invalid-fan-reports/**：废号审核
- **group-customers/historical**、**expert-customers/historical**：历史客户补录（阶段6要改成组长专属+审核门禁）
- **exports/**：attendance、member-daily、member-performance 导出
- **notifications/**、**attendance/**、**daily-confirmations/**、**device-accounts/**、**events/**、**history/**：辅助功能
- **~~reports/~~**：已删（阶段0清理，零调用方，[7ea5bff](../../../../7ea5bff)）

---

## v2 原型 → 需求文档 映射（前端参考，冲突以需求文档为准）

| v2 原型概念 | 对应需求文档章节 | 已知分歧 |
|---|---|---|
| `DownstreamLead.expertStage`(8段) | 第八章 | v2 的"杀不动"沿用旧语义(需已开单)，需求文档8.2已重定义为"未成交/停止维护" |
| `CLAIM_BASELINE_STAGES`(炒群只能认领JOINED) | 7.3 | 一致 |
| 各岗位自助认领老客户 | 7.3 | 需求文档要求统一收口给组长审核，v2是各岗位自助 |
| `MoneyEvent`(首充/续充/出金统一数组) | 2.4 | 大体一致，真实后端是 `CustomerOrder`+`MetricEvent` 分离 |
| `FUNNEL_CHIPS` 当期比值口径 | 6.1 | 需求文档要同批口径，v2也是当期比值，两边都要改 |
| `RECEPTION_BY_GROUP_OPERATOR`(组长配对) | 1.4 | 一致 |
| 认领/录入时的"发生日期"字段 | 隐含在7.3的日期校验精神里 | 一致，是好的参考实现 |

---

## 重写进度

见 `/Users/aaaa/.claude/plans/merry-sauteeing-cook.md` 的"分阶段执行"一节。当前状态：阶段0、阶段1已完成。

**阶段1完成内容**：验证专家状态跳转（符合8.2语义，无需改动）+ 验证并修复无效库改判校验（与3.1.1冲突，已放开，见上）；新增 `Company`/`Position`/`Duty`/`UserPosition`/历史补录审核字段（纯 schema 新增，均无消费方，阶段2/5/6会陆续接线）。SQLite 迁移 `20260828133105_add_company_position_duty_historical_review` 已应用到 `prisma/dev.db`；对应的 Postgres 迁移已手写并登记进 `checksums.json`（**未在真实 Postgres 实例上跑过 `prisma migrate dev` 生成，是照现有迁移风格手写的，接入生产库前建议先在测试库跑一遍 `db:postgres:test:prepare` 验证 SQL 语法**）。
